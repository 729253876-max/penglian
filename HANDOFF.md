# Project-002 修图 AI 小程序｜项目记忆与交接文档

> 文档用途：持续记录用户与 Codex 在产品、商业、设计和技术层面的关键结论，供后续提炼、恢复上下文和人员交接使用。  
> 当前版本：v03.4
> 创建日期：2026-07-21  
> 最近更新：2026-07-29
> 当前状态：阶段 A 环境验收已完成并集成；阶段 B0 设计收敛中，尚未进入实施；产品仍不可发布
> 项目绝对路径：`D:\Documents\workspace\projects\Project-002-修图AI小程序`

## 0. 新对话快速接管

### 0.1 当前基线

- 规范项目：`D:\Documents\workspace\projects\Project-002-修图AI小程序`
- 阶段 A 验收分支：`codex/v1-phase-a-env-acceptance`，已于 2026-07-28
  纯快进合并至 `master`。
- 阶段 B0 分支创建基线：`ba54150`；其中阶段 A 环境验收合并基线为
  `05b8155`，后续提交归档了审美共创候选功能。HANDOFF 集成可能使
  `master` 继续前进，新对话接管时必须以
  `git status --short --branch` 和 `git log -1 --oneline` 的实际输出为准。
- 阶段 A 核心代码提交：`cc187d0`（最终审查问题修复）。
- 阶段 B0 设计分支：`codex/v1-phase-b-readiness-design`。
- 阶段 B0 独立 worktree：
  `D:\Documents\workspace\projects\Project-002-修图AI小程序\.worktrees\v1-phase-b-readiness-design`。
- 该 worktree 从 `ba54150` 创建；截至本次交接，仅进行设计决策与交接文档整理，
  尚未编写阶段 B 业务代码、开通腾讯云服务或产生云资源费用。
- 当前结论：**阶段 A 的 3100、微信编译、9420、SVG 与开发者工具版本 GUI
  证据已齐，验收分支已完成提交、复核与纯快进集成，阶段 A 环境验收完成。
  用户已授权继续阶段 B0 设计收敛，但尚未批准完整设计或阶段 B 业务实施；
  产品仍不可发布。**
- 阶段 A 当前只开放“AI 智能人像精修”纵向体验；画质增强、路人/杂物消除和
  老照片修复仅保留共享契约及“阶段 B 接入”的禁用入口。

### 0.2 已完成与验证证据

- Task 1–9 已完成，不得在新对话中重新派发或重做。
- 最终全分支审查发现的 C1/C2、I1–I5、M1–M3 已全部修复，并通过限定复审；
  没有遗留 Critical 或 Important。
- 规范主工作区重建依赖后，TypeScript 类型检查通过。
- 新增真实 HTTP 冒烟后，Vitest 共 9 个测试文件、125 项测试通过。
- `npm.cmd run verify:miniprogram-runtime` 通过，证明小程序 service 不依赖未构建的
  外部运行时 npm 模块。
- 2026-07-27 新增 socket 级测试并真实绑定 `127.0.0.1:3100`；五类 HTTP
  状态与事件末项均通过。
- 2026-07-27 在当前 `master` 小程序源码上重新编译两次；`compiled: true`，
  `errors: []`，`wxml_errors: []`。
- 环境验收分支修复真实 IDE 工程根、WXSS 兼容和空 body POST `415` 后，
  全量测试为 `10/10` 文件、`129/129` 测试；类型检查、无外部模块验证和
  8 个微信 JS 构建产物均通过；新增测试会把 TypeScript 编译到临时目录并
  逐一比对仓库内生成物，防止提交过期 JS。
- 验收分支通过 `3f801f0`、`90d3c41`、`05b8155` 三次纯快进合并至
  `master`。后两次提交分别让生成物新鲜度测试忽略等价换行差异，并通过
  `.gitattributes` 固定微信生成 JS 为 LF，消除 Windows `core.autocrlf=true`
  引起的误报。
- 2026-07-28 在最终 `master` `05b8155` 上重新执行类型检查、全量 Vitest、
  无外部模块运行时验证、微信 JS 构建和生成物新鲜度测试：`10/10` 测试文件、
  `129/129` 测试与定向 `3/3` 均通过。真实 3100 冒烟前后均无
  `LISTENING` 残留，`git diff --check` 通过，主工作区保持干净。
- 2026-07-27 微信 IDE 编译为 `compiled: true`、`errors: []`、
  `wxml_errors: []`、`automator_verified: true`；2026-07-28 提交前再次编译
  仍无源码/WXML 错误，但本次 `automator_reconnected: false`，不能据此重写
  此前 9420 结论。
- 2026-07-28 在规范 `master` 运行时目录重新执行真实 IDE 编译：
  `compiled: true`、`errors: []`、`wxml_errors: []`，
  `Using AppID: touristappid`。编译后的 9420 快速复核为 TCP 已监听但
  WebSocket 未就绪，两次 `page_data` 均在连接层失败；按重试上限停止。
  该结果不构成新的交互通过证据，也不覆盖 2026-07-27 已完成的直接运行时证据。
- 9420 主链路、9 条连续事件重入、失败提示、减少动态效果持久化、水印预览
  和两张 SVG 模拟器截图均已验证。
- 2026-07-28 从真实项目窗口打开“微信开发者工具 → 当前版本”，直接 GUI
  显示 `2.02.2607171 RC win32-x64`；证据截图为
  `06-复盘与踩坑/阶段A-微信开发者工具-当前版本-20260728.png`。本 RC
  版本的“关于”菜单项会打开官网概览页，版本弹窗入口实际为“当前版本”。
  完整项目窗口与“关于 / 当前版本”菜单入口的组合截图为
  `06-复盘与踩坑/阶段A-微信开发者工具-版本入口-20260728.jpg`。
- API 仓储为内存态，重启后旧任务不能恢复；本轮没有宣称恢复成功。

### 0.3 阶段边界与下一步

- 阶段 A 环境验收已完成；不得把这一结论扩大为“产品可发布”。
- 阶段 B 已进入 B0“架构与合规就绪”的设计收敛，但尚未完成正式设计规格，
  也尚未进入实施。后续 B1–B4 仍需按独立功能/验收目标继续使用新的
  `codex/*` 分支与独立 worktree。
- 真实用户图片、供应商、账户、支付、积分、审核、持久化与发布链路尚未接入。
- `touristappid` 只证明本地编译和模拟器验收，不证明真实项目身份、真机预览、
  上传、微信登录、云开发或支付。
- 正式微信 AppID 已由用户提供：`wx4f7678cc595d276b`。当前仓库两个微信工程
  配置仍为 `touristappid`；只能在 B1 实施时统一替换并重新编译、预览和真机
  验证。AppSecret、腾讯云密钥、供应商密钥不得写入仓库或 HANDOFF。
- 图像处理供应商尚未确定。该状态不阻塞 B0、B1、B2 的设计和可替换接口实现，
  但阻塞 B3 真实供应商接入、真实质量/费用/延迟验收和生产发布判断。
- 不得重复执行供应商评测；该工作属于独立专项任务。

### 0.4 新对话首条指令

复制以下内容作为新对话的第一条消息：

```text
请完整读取
D:\Documents\workspace\projects\Project-002-修图AI小程序\HANDOFF.md，
继续 Project-002 修图AI小程序。

先进入 Git 仓库根目录
D:\Documents\workspace\projects\Project-002-修图AI小程序，
再只读核对 git status --short --branch、git log -1 --oneline、
06-复盘与踩坑/V1阶段A验证记录-v01-20260726.md 和
04-源文件与代码/README.md。当前 Task 1–9 与最终审查修复已经完成，
不要重做；供应商评测属于独立任务，不要重复执行。

阶段 A 环境验收已完成；阶段 B0 分支创建基线为 `ba54150`，当前 `master`
必须以实际 Git 输出为准。阶段 B0 设计位于
`codex/v1-phase-b-readiness-design` 和
`.worktrees/v1-phase-b-readiness-design`。请先核对主工作区及该 worktree
的 Git 状态和本文档，不要重做 Task 1–9、环境取证或供应商评测。

继续阶段 B0 头脑风暴：已确认腾讯云统一托管、上海地域、数据保存与删除 SLA、
按需微信登录、正式 AppID 和 COS 全私有访问。下一步先继续收敛剩余关键边界，
再提出 2–3 个完整方案、分节提交设计、取得用户确认并写正式设计规格；规格获批
前不得写阶段 B 业务代码。供应商未定不阻塞 B0–B2，但不得进入 B3 真实接入，
也不得宣称产品可发布。
```

## 1. 维护规则

1. 每次出现产品定位、版本范围、收费方式、关键交互、技术架构、外部依赖或重大风险变化时，同步更新本文档。
2. 只把用户明确确认过的内容写入“已确认决策”；尚未确认的建议统一放入“待确认问题”。
3. 新结论与旧结论冲突时，不静默覆盖：修改正文口径，并在“变更记录”注明日期、旧口径和新口径。
4. 本文档用于快速恢复上下文，不替代正式 PRD、技术设计、接口文档和测试用例。
5. 若本文档与旧版 PRD 冲突，以本文档中日期更晚且标为“已确认”的决策为当前口径；正式开发前必须把差异同步进新版 PRD。
6. 项目所有有效文件只允许保存在本项目标准目录内。
7. 后续功能默认使用 `codex/*` 功能分支和独立 worktree；一个功能/验收目标
   对应一个工作树，避免不同任务共享未提交状态。集成前必须复核基线、测试、
   变更范围和目标分支，不在单一长期工作树中混做多个功能。

## 2. 项目一句话定位

一款面向普通消费者的微信 AI 修图工具：不要求用户学习提示词，在忠实保留人物身份和原图构图的前提下自动完成专业精修，并用可理解的过程记录与参数报告帮助用户学习修图。

## 3. 商业目标与产品原则

### 3.1 商业目标

- 首先保证产品足够好用、有明显效果、能够吸引用户并形成可持续收入。
- 不与豆包、千问等通用 AI 应用正面比拼模型规模和免费额度，而是依靠垂直体验、按张付费、忠实精修和个人审美记忆形成差异化。
- 核心传播方向：“无需开长期会员，一张重要照片也能获得专业精修。”
- 收费必须透明，用户在生成前明确知道本次消耗、输出规格及解锁内容。

### 3.2 产品原则

- **忠实修图优先**：默认不是重新生成一张新图片，不改变人物身份、五官结构、姿势、服装、主体数量和主要构图。
- **真实过程展示**：展示可验证的处理阶段、实际参数变化和结果检查，不伪造或暴露模型的隐藏推理过程。
- **先证明效果再收费**：允许用户看到不可保存的低清水印预览，再决定是否解锁高清结果。
- **低学习成本**：默认自动诊断和一键修复，同时提供独立的手动参数表。
- **隐私可控**：个人审美档案需明确授权，可暂停、查看和清除；默认不使用用户照片训练公共模型。

## 4. 已确认的关键决策

### 4.1 首发用户与拳头场景

- 首页与首轮推广以“人像生活照”为主，包括自拍、旅行照、家庭照和日常分享照片。
- 选择该场景的原因：使用频率和复购潜力高，且适合展示 AI 诊断与教学过程。

### 4.2 V1 首发能力

首版正式提供四项能力，并优先把成片质量做深：

1. AI 智能人像精修。
2. 画质增强。
3. 路人和杂物消除。
4. 老照片修复。

AI 抠图和创意编辑不进入 V1。用户已于 2026-07-21 确认：创意模式正式放到第二阶段，在 V1 商业数据稳定后再单独设计和上线。

### 4.3 修图边界

- 使用成熟的图像处理/图像编辑 API，而不是文本生成图片模型作为默认主链路。
- AI 负责照片诊断、参数计算、局部蒙版、人脸与主体识别、自然美颜、降噪、锐化、细节修复和小范围背景补全。
- 路人或杂物消除允许在用户指定的小区域补齐背景，但不得无提示修改其他区域。
- 老照片修复默认处理划痕、褪色、噪点、轻度模糊和局部破损，必须忠实保护人物身份、服饰、年代感与原构图。
- “修复并上色”只能由用户主动选择并完成显式确认；默认不触发上色。缺乏真实依据的生成性细节不得宣称为“真实历史还原”。
- 输出前进行人物身份、主体和构图一致性检查；不合格结果自动重试或按失败规则处理。
- 默认“忠实精修”和未来“创意增强”必须在产品入口、文案和计费上明确区分。

### 4.4 修图过程与教学报告

- 修复过程中采用类似流式回答的视觉体验，持续展示真实执行阶段。
- 未解锁高清前，可以展示：发现的问题、处理阶段、调整方向、部分原因和部分教学内容。
- 未解锁高清前，需要隐藏：完整步骤、精确参数值、完整 PS/Lightroom 对照及完整教学报告。
- 锁定内容必须明确显示“高清解锁后查看完整参数”，不能用虚假内容冒充真实过程。
- 高清解锁后提供：完整步骤、实际参数、调整原因、效果影响和 PS/Lightroom 对应操作位置。
- 用户不需要从过程步骤直接修改并重新生成，但编辑页面要提供独立的参数表，允许用户在提交前调整强度和专业参数。

### 4.5 个人审美档案

- 个人审美档案是 V1 核心留存能力。
- 记录用户偏好的自然程度、肤色冷暖、画面明暗、色彩倾向和风格选择。
- 通过用户选择的预览方案、高清解锁、收藏及“不喜欢”反馈逐步更新偏好。
- 用户可以查看、暂停学习和一键清除审美档案。
- 默认不使用用户照片训练公共模型。

### 4.6 收费与免费体验

- 新用户前 3 次可免费获得 AI 诊断和低清水印预览。
- 低清水印预览只能在小程序内查看，不允许保存到手机。
- 用户满意后再解锁高清无水印结果和完整参数报告。
- 不让用户连续拉起两次微信支付。
- 前台同时支持充值积分和直接购买本次结果；后台统一通过积分账本结算。
- 直接购买时，由系统完成“购买本次所需积分并立即消费”，用户感知为一次支付。
- 如果正常用户的预览阶段需要消耗积分，该消耗必须抵扣同一任务的高清解锁费用，不能重复收费。
- “标准高清不超过 1 元”不再作为已冻结上限。用户在成本分析后认为长期固定 ¥0.99 可能不合理，但担心普通消费者在不了解忠实精修价值时不愿为单张照片支付超过 1～2 元，因此最终价格必须同时验证用户支付意愿、预览转化率和 API 实际成本。
- 原图高清、超清增强等高成本规格不受 1 元上限约束，可提高费用，但必须由用户主动选择并在生成前透明展示。
- 暂不把连续包月会员作为首发主要收入方式，先通过真实复购数据验证需求。
- 积分制必须保留；前台可同时提供直接支付与积分支付，后台继续统一使用积分账本。
- 用户接受冷启动阶段为高质量获客承担可控的小额亏损，但要求获得的用户具有较高付费转化和复购潜力，后期必须实现单位经济转正，不能依赖永久补贴生存。

免费预览的已确认计费边界：

- 新用户免费次数只在“成功生成可查看的水印预览”后扣减；上传失败、审核拒绝、系统失败和主动退出不扣免费次数。
- 前 3 次免费额度用完后，用户可以选择观看一次激励视频换取一次水印预览，或使用少量积分生成预览。
- 预览阶段不单独拉起微信支付；用户若没有积分，应直接选择购买并解锁最终规格，避免连续支付。
- 同一任务已消耗的预览积分必须全额抵扣高清解锁积分。
- 预览积分数值、广告每日上限和补差有效期仍需在定价方案中冻结，不得在代码中写死。

### 4.7 广告策略

- 只接入用户主动选择的微信激励视频广告。
- 不做开屏广告、强制插屏或修图过程中的打断式广告。
- 免费次数用完后，用户可通过观看激励视频获得一次额外水印预览或少量积分。
- 广告不能直接解锁高清结果和完整参数报告，避免冲击付费价值。

### 4.8 输出规格策略

- 输出规格由用户主动选择，不强制所有用户使用同一分辨率。
- 分辨率越高，AI计算、存储和带宽成本越高，可以采用分档定价。
- 免费水印预览采用受限分辨率，只用于判断效果，不作为正式交付文件。
- 标准高清、原图高清、超清增强的具体像素上限和价格仍需结合 API 实测成本后冻结。

### 4.9 AI 服务建设方式

- 首版直接使用成熟、可商用的图像处理 API 上线，不从零训练自有模型。
- 服务端建立统一供应商适配层，小程序不能直接依赖某一家供应商的字段和流程。
- API 选型优先考虑：成片质量、身份一致性、国内可用性、数据合规、单张成本、处理时长、失败率和可替换性。
- 订单量和质量数据稳定后，再评估自部署模型降低成本。

### 4.10 登录与开发环境

- 在真正需要微信登录、AppID、支付、预览上传或真机测试时再提示用户操作。
- 本机已检测到：微信开发者工具、Node.js `v24.18.0 LTS`、npm `v11.16.0`、Git、VS Code 和微信开发者工具 MCP。
- PowerShell 直接执行 `npm` 会受脚本策略影响，开发命令优先使用 `npm.cmd`。
- 当前不要求安装 Python；若未来选择 Python 图像服务再单独评估。

### 4.10.1 AI 精修实况

- 用户已于 2026-07-24 确认将“类似 AI 流式回答的真实修图过程”提升为 V1 一级能力，正式命名为“AI 精修实况”。
- 该能力展示真实照片诊断、推荐方案、参数调整方向、处理阶段、质量检查、自动重试和交付事件。
- 视觉体验可以采用逐条出现、阶段推进、内容展开和局部对比，但不得复制第三方品牌，也不得输出或伪造模型隐藏推理。
- 所有展示内容必须可以追溯到平台或供应商实际产生的结构化任务事件。
- 未解锁前展示问题、方向、阶段和部分教学；高清解锁后提供完整参数、原因、效果、蒙版与 PS/Lightroom 对照。
- 服务端建立统一 `EditTrace` 事件层，小程序不得用计时器或固定脚本编造修图步骤。

### 4.10.2 AI 推荐与用户确认

- 默认由 AI 诊断并提供一个推荐方向，最多提供两个忠实范围内的备选方向；用户不需要编写提示词。
- 人像精修允许用户确认自然程度、明暗和冷暖倾向；画质增强由系统推荐规格，用户确认用途与交付档位。
- 路人和杂物消除可以由 AI 给出候选目标，但最终目标和蒙版必须由用户确认。
- 老照片修复必须记录默认修复范围；如用户请求“修复并上色”，必须同时记录上色请求与显式确认。EditTrace 仅展示真实诊断、已确认选项、实际参数和处理阶段，不展示隐藏推理。
- 个人审美档案在首次成功交付后自愿开启，不采用阻断式长问卷。

### 4.10.3 精准引流与增长

- 用户已确认增长体系必须纳入产品一级设计，不能只作为上线后的运营备注。
- 推荐增长主张为“重要照片可以修得更好，但不应该修得不像你”。
- 采用“真实案例内容 → 场景工具直达 → 成功预览 → 高清解锁 → 用户主动分享 → 好友进入同一方向 → 复购”的效果证据闭环。
- 小红书、抖音、视频号和微信生态为主要内容与分发渠道；达人和渠道合作优先按首次高清解锁或 30 天贡献毛利结算。
- 分享必须由用户主动选择展示内容，不强制给付费成片添加推广水印。
- 邀请奖励不按点击或注册发放高价值权益；优先绑定好友首次高清解锁，并支持防刷、冻结和退款撤销。
- 有效新用户定义为“成功生成可查看水印预览的真实用户”，不能只以访问或注册计数。
- 推荐冷启动主题为“1000 张重要照片忠实精修计划”，不承诺免费交付 1000 张高清结果。

### 4.11 定价候选方案与当前边界

以下为 2026-07-22 形成的推荐方案，尚未完成 API 报价和用户价格测试，不能视为最终定价：

- 新人第一次高清解锁候选价：¥0.99；通过系统补贴积分与用户购买积分共同完成，不连续拉起两次支付。
- 后续标准人像精修、标准画质增强候选价：¥1.99 或 2 积分。
- 路人/杂物消除候选价：¥2.99 或 3 积分起，根据蒙版面积、背景复杂度和输出规格在生成前透明报价。
- 原图高清、超清增强额外计费；积分包可提供小幅阶梯优惠，但不得通过过度折扣重新形成永久 ¥0.99 锚点。
- ¥0.99 更适合承担新人首单、流失召回、邀请奖励或限量活动的获客角色，而不是所有工具的永久统一价格。
- 初期经营重点指标为预览成功后的高清解锁率、首单用户 30 天复购率和 30 天用户贡献毛利；价格是否生效必须由真实 API 成本与灰度数据共同决定。
- 每次生成一套、三套或“一套 AI 加两套客户端模拟”的预览策略尚未确认；该选择推迟到供应商单价和预览转化模型明确后决定。

### 4.12 后期候选：审美共创互动积分

- 用户已于 2026-07-28 确认：该能力是后期候选增长功能，现阶段不开发，
  不进入阶段 B，也不作为 V1 核心链路的前置条件。
- 综合目标为：在积分不足时提供继续体验路径、提高回访留存、收集结构化审美
  反馈；三者通过同一任务闭环实现。
- 首版只使用平台授权样本图，不使用或展示真实用户投稿，避免把私人照片交给
  其他用户评测。
- 任务采用“二选一 + 一个结构化原因标签”，不做自由评论、私信、关注关系
  或开放式问答。
- 奖励可兑换低清预览，并最多抵扣高清解锁费用的 20%–30%；不能无限兑换
  高成本高清结果，也不能在用户之间转移。
- 未来不能仅凭问卷投票开发；必须依次通过意愿投票、假门行为测试和平台样本
  小流量任务实验。完整边界见
  `02-方案与设计/审美共创互动积分候选功能-v01-20260728.md`。

### 4.13 阶段 B0 已确认架构与合规边界

以下决策由用户于 2026-07-28 至 2026-07-29 逐项确认，属于阶段 B 正式设计的
输入；目前尚未写成完整设计规格，也尚未授权实施：

- 阶段 B 按独立可验收单元拆分，不作为一个大任务一次实施：
  1. B0：架构与合规就绪。
  2. B1：微信身份与授权。
  3. B2：上传会话、COS 和内容审核。
  4. B3：供应商适配与主备路由。
  5. B4：质量检查、失败恢复、删除与数据保留。
- 基础设施采用“腾讯云统一托管”方向：
  - 腾讯云 CloudBase 云托管运行现有 Node.js/Fastify 服务。
  - 腾讯云 COS 保存图片对象并支持签名直传。
  - 腾讯云 MySQL 保存用户、授权、上传会话与任务。
  - 腾讯云图片内容安全承担上传图片审核。
  - Redis 和消息队列不在首轮预置，仅在真实并发、削峰或可靠重试证明需要时加入。
  - 业务层必须保留 `ObjectStorage`、`ContentModerator`、`TaskQueue` 和图像处理
    供应商适配接口，避免把业务逻辑锁死在某一云产品或供应商字段中。
- 优先使用国内网络可直接访问、中文管理界面完整且运维集中的产品；设计获批并
  进入实施前，不开通服务、不创建计费资源。
- 腾讯云首选地域为**上海**。CloudBase、COS、MySQL 和内容安全应尽量同地域；
  正式开通前必须核对各产品在上海的实际可用性、网络路径和计费，不能只凭设计
  假设资源一定可开通。
- 图片保存期限采用平衡方案：
  - 原图 7 天。
  - 低清预览 7 天。
  - 付费结果 30 天。
  - 用户主动删除后立即在产品中隐藏并禁止继续访问。
  - 业务库索引与 COS 活跃文件在 24 小时内删除。
  - 备份最长 30 天自然淘汰，期间不得恢复到线上。
  - 平台立即向供应商发起删除；供应商必须提供明确的数据保存和删除期限。
- 登录采用按需触发：
  - 首页和功能说明允许未登录浏览。
  - 用户首次上传图片时展示隐私说明，确认后再调用 `wx.login` 建立服务端会话。
  - V1 不强制获取头像、昵称或手机号。
  - 上传、任务记录、积分和付费结果绑定平台内部用户 ID。
- 正式微信 AppID 为 `wx4f7678cc595d276b`。当前两个微信工程配置中的
  `touristappid` 只用于既有阶段 A 本地验证；B1 实施时必须统一替换，且通过
  正式项目编译、预览和真机验证。AppSecret 只能通过部署环境的密钥管理注入，
  不得发送到对话或写入 Git。
- COS 使用全私有访问：
  - 存储桶禁止公开读写，V1 暂不引入 CDN。
  - 服务端只签发短时、用途受限的上传凭证和下载地址。
  - 服务端必须同时验证用户、上传会话、对象路径、文件大小和文件类型，不能只
    依赖签名 URL。
  - 用户只能访问属于自己的对象。
- 图像处理供应商当前未确定，也没有测试账号。主线可以先完成 B0–B2，并使用
  接口抽象、模拟适配器和契约测试隔离依赖；到 B3 真实接入前必须取得候选供应商、
  测试权限和数据删除条款。没有真实供应商证据时，不得宣称质量、成本、时延、
  失败恢复或生产全链路已经验收。

## 5. V1 目标商业漏斗

以下为四工具全部接入后的 V1 目标漏斗。阶段 A 当前只有人像精修入口可用；
画质增强、路人/杂物消除和老照片修复仍是禁用入口，不得把目标漏斗误报为
阶段 A 已实现范围。

```text
进入小程序
→ 选择人像精修/画质增强/杂物消除/老照片修复
→ 上传照片
→ 免费或低成本 AI 诊断
→ 流式展示部分真实处理步骤
→ 生成不可保存的低清水印预览
→ 用户比较效果
→ 使用积分或一次直接支付解锁
→ 获得高清无水印成片 + 完整参数报告
→ 用户评价/收藏风格
→ 更新个人审美档案
→ 下次按个人偏好自动精修
```

## 6. PRD 基线与历史差异

当前有效 PRD：`01-需求与目标/修图AI小程序-PRD-v02-20260724.md`。
`PRD-v01-20260719.md` 仅作为历史基线保留。

PRD v01 之后截至 2026-07-24 新增或调整的内容如下。此处的“三项工具”是历史决策，已由 2026-07-26 的四项 V1 工具决策覆盖：

- 产品差异化升级为“忠实 AI 精修 + 修图教学 + 个人审美档案”。
- 首发范围从五项工具收敛为三项：人像精修、画质增强、路人杂物消除。
- 增加流式修图过程、参数分层隐藏和完整教学报告。
- 增加不可保存的低清水印预览及高清补差解锁机制。
- 支付前台支持积分与直接购买，后台统一积分账本。
- 增加仅奖励式广告策略。
- 输出分辨率改为用户选择并按成本分档收费。
- 增加个人审美档案及其授权、反馈和清除机制。
- AI 路线明确为成熟图像编辑 API + 统一供应商适配层。

以上历史差异已经生成并同步至 PRD v02；后续开发必须以 PRD v02、日期更晚的
已确认决策和本交接文档为当前口径，不得恢复 v01 的旧范围或旧定价。

## 7. 待确认问题

以下内容尚未由用户最终确认，不能作为硬编码依据：

1. 标准高清、原图高清、超清增强的具体分辨率上限、积分和现金价格；当前候选为新人首单 ¥0.99、后续标准任务 ¥1.99/2 积分、杂物消除 ¥2.99/3 积分起，但均须通过 API 报价和价格测试冻结。
2. 第 4 次起生成预览所需积分、激励广告每日上限和同任务补差有效期。
3. 四类 V1 工具分别接入哪家成熟 API，以及主备供应商组合。
4. AI 单张综合成本上限和目标毛利率；不能再仅按名义 API 调用价估算，必须按“每张成功交付结果的综合成本”计算免费预览摊销、失败重试、审核、存储、带宽、支付、退款和税费。
5. 用户审美档案的首次问卷形式、偏好维度和学习权重。
6. 腾讯云 CloudBase 云托管、COS、MySQL 和内容安全在上海地域的实际产品
   可用性、账号开通条件、网络路径和计费；统一托管方向已确认，但尚未开户或实测。
7. 产品正式名称、品牌视觉和首页核心文案。
8. 免费额度的设备/账号防刷规则。
9. 手动参数表的最终字段、范围、默认值和高级模式边界。
10. 第二阶段创意模式的具体功能范围、成本和上线条件；其阶段归属已经确认，不阻塞 V1。
11. 每次任务生成一套、三套还是“一套 AI 加两套模拟”预览；从当前单位经济模型看一套真实预览最可控，但用户尚未最终选择。
12. 首单补贴预算、价格灰度周期、预览解锁率最低线、30 天复购率和停止投放条件。
13. B2 首批允许上传的图片格式、单文件大小、像素上下限和异常图片处理规则。
14. 内容审核拒绝、疑似违规、审核服务不可用时的用户提示、人工复核与是否扣次。
15. B1 服务端会话有效期、换机/多端策略和账号注销流程的具体时限。
16. 供应商候选、测试账号和数据删除条款；当前未确定，不阻塞 B0–B2，但阻塞
    B3 真实接入和任何生产发布判断。

## 8. 当前项目文件

- `README.md`：项目入口和治理信息。
- `AGENTS.md`：项目级写入边界与执行规则。
- `HANDOFF.md`：本项目持续维护的关键决策与交接入口。
- `01-需求与目标/修图AI小程序-PRD-v01-20260719.md`：历史 PRD 基线，
  不是当前开发依据。
- `01-需求与目标/修图AI小程序-PRD-v02-20260724.md`：当前有效 PRD 基线；
  2026-07-26 确认的四工具范围及其后决策由本 HANDOFF 补充，后续应同步至
  新版 PRD。
- `02-方案与设计/修图AI小程序-V1产品增长与AI精修实况设计-v01-20260724.md`：已于 2026-07-24 经用户书面复核确认的正式设计规格。
- `02-方案与设计/审美共创互动积分候选功能-v01-20260728.md`：已确认的后期
  候选增长功能档案；现阶段只归档，不进入代码或阶段 B。
- `02-方案与设计/实施计划/00-V1实施路线图-20260724.md`：将 V1 拆分为六个可独立验收阶段。
- `02-方案与设计/实施计划/2026-07-24-V1基础与AI精修实况纵向切片实施计划.md`：阶段 A 详细实施计划，先使用模拟供应商打通真实事件流和水印预览。
- `04-源文件与代码/`：阶段 A 原生小程序、Node.js API、共享契约与自动化
  验证工程；阶段 A 环境验收已快进合并至 `master`，验收合并基线为
  `05b8155`。阶段 B0 分支创建基线为 `ba54150`；当前 `master` 以接管时
  `git log` 为准。
- `04-源文件与代码/README.md`：依赖、启动、类型检查、测试、小程序导入和
  无外部运行时模块验证方式。
- `06-复盘与踩坑/V1阶段A验证记录-v01-20260726.md`：阶段 A 的永久验证证据、
  环境阻塞、未完成清单和阶段 B 进入条件。
- `.worktrees/v1-phase-b-readiness-design/`：阶段 B0 设计独立 worktree；
  分支为 `codex/v1-phase-b-readiness-design`，不得把其中未提交或未审阅内容
  描述为已进入 `master`。

### 并行专项任务

- 任务名称：`[Project-002] 图像API供应商评测与阶梯报价`。
- Codex 任务 ID：`019f8a41-ac4a-7780-b0bf-d3f99eb77efa`。
- 任务状态：2026-07-22 已创建，正在按 Project-002 规则进行只读接管与公开信息调研。
- 任务目标：分别覆盖人像精修、画质增强、路人/杂物消除和老照片修复四类 V1 能力的主备供应商；统一阶梯询价口径；设计同批样片盲测；核算每张成功交付结果的综合成本。
- 权限边界：公开调研、询价模板、测试方案和报价对比可由 Codex 完成；登录、短信验证码、企业资料、对外提交询价、合同、付款和最终商务承诺必须由用户确认。
- 本次文档范围修订不执行供应商评测、报价或选型；供应商专项不改变已冻结的四项 V1 产品范围。

## 9. 下一步建议顺序

1. 阶段 A 环境验收已完成并集成，不重做 Task 1–9、最终审查修复、供应商评测
   或阶段 A 环境取证。
2. 继续在 `codex/v1-phase-b-readiness-design` 独立 worktree 完成阶段 B0
   头脑风暴。首先确认 B2 上传格式/大小等剩余边界；问题一次只问一个。
3. 边界足够后提出 2–3 个完整方案并说明取舍，分节提交架构、组件、数据流、
   错误处理和测试设计，逐节取得用户确认。
4. 用户确认完整设计后，把规格写入 `02-方案与设计`，自查占位符、矛盾、
   范围和歧义，提交到阶段 B 设计分支并请用户书面审阅。
5. 规格书获用户批准后再编写实施计划；阶段 B 实施继续按 B0–B4 拆分，并为
   每个功能/验收目标使用独立 `codex/*` 分支和 worktree。
6. 供应商未定不阻塞 B0–B2；B3 只能先定义抽象、模拟适配器和契约测试。取得
   候选供应商、测试权限和删除条款前，不进行真实接入或生产验收。
7. 腾讯云资源尚未开通；设计获批并得到实施授权前，不创建计费资源。
8. 审美共创只保留为后期候选，不得插入当前主线。

## 10. API 样片评测基线

以下为产品侧交给独立供应商专项的评测起点，不代表最终上线承诺，也不授权本对话
重复执行供应商调研、询价或选型：

- 任务成功率目标不低于 95%，生成时长 P90 不高于 60 秒。
- 人像精修必须覆盖单人、多人、侧脸、弱光、逆光、戴眼镜和复杂背景样片。
- 重大身份漂移、五官变形、肢体新增、服装结构改变和非目标区域大面积变化均判定为不合格。
- 人像精修重点人工评分：身份一致性、肤质自然度、明暗层次、色彩舒适度和整体可用性。
- 画质增强重点检查：伪文字、重复纹理、过度锐化、塑料肤质和不存在细节的强行生成。
- 杂物消除重点检查：蒙版边缘、透视、纹理连续性、阴影和非目标区域稳定性。
- 老照片修复重点检查：人物身份、服饰、年代感和原构图是否稳定；划痕、褪色、
  噪点、轻度模糊及局部破损是否得到忠实修复；上色必须单独评测且只在用户主动
  请求并显式确认时触发，不得把生成性细节宣称为真实历史还原。
- 每家候选 API 使用同一批匿名测试图、同一输出规格和同一评分表；质量、成本、时延和失败率共同决定主备路由。
- 正式阈值、样本数量和自动化指标需要在技术设计中冻结。
- 历史三工具建议曾为每家供应商约 110 张：人像精修 50 张、画质增强 30 张、
  杂物消除 30 张。该数量不再是四工具评测的当前冻结值；老照片修复加入后的
  样本分配、总量和阈值由独立供应商专项统一确定。如某供应商只提供单项能力，
  则仅评测对应样片组。
- 阶梯询价统一覆盖月调用量 1 千、1 万、5 万、10 万、50 万张；预览 720P/1080P；正式结果 2MP/4MP/原图。
- 必须询问失败、超时、审核拒绝、自动重试是否收费，套餐有效期、最低充值、并发限制、图片保存期限和立即删除能力。
- 公开价格只能用于初筛，最终商业判断以具有明确规格、调用量和服务条款的书面报价为准。

## 11. 交接时必须说明的风险

- 售价低于 1 元时，AI推理、内容审核、存储、带宽、支付和获客成本可能吞噬利润；必须在 API 评测后校准价格。
- 免费预览必须能证明效果，但不能成为可替代高清付费结果的正式文件。
- 过程展示只能基于真实任务事件和参数，不得输出虚构的模型思考过程。
- 人像精修必须重点验证身份一致性、自然肤质、五官稳定和多人照片表现。
- “忠实精修”和“创意生成”不能混用同一承诺，否则容易造成用户信任和投诉问题。
- 个人审美档案属于长期用户数据，必须取得授权并支持查看、暂停和删除。
- PRD v02 是当前有效基线；不得把 PRD v01 中已废弃的旧价格、五工具范围或其他
  历史口径重新写入代码。
- ¥0.99、¥1.99、¥2.99 目前都是候选商业结构，不是已冻结商品价格；不得在供应商报价和灰度验证前写死。
- 用户愿意承担的是有止损线、可通过复购收回的首单补贴，不是长期无上限亏损；必须分开核算首单、复购、免费预览和获客渠道成本。
- “效果追平或超过主流平台”必须通过同图盲测、身份一致性和严重缺陷率验证，不能仅凭供应商宣传或单次样片判断。

## 12. 变更记录

| 日期 | 版本 | 变更内容 | 状态 |
|---|---|---|---|
| 2026-07-21 | v01 | 创建项目记忆与交接文档；汇总此前关于定位、首发范围、过程展示、收费、广告、分辨率、审美档案和 AI 服务路线的讨论 | 已记录 |
| 2026-07-21 | v01.1 | 明确创意模式不进入 V1、1 元上限只适用于标准高清；补充免费预览扣次边界和 API 样片评测基线 | 已记录 |
| 2026-07-21 | v01.2 | 用户确认创意模式放到第二阶段；移除其版本归属不确定性 | 已确认 |
| 2026-07-22 | v02.0 | 取消“标准高清永久不超过 1 元”的冻结口径；确认积分制保留、允许可控首单补贴但后期必须盈利；记录候选定价结构、成本核算原则和预览方案待决状态 | 部分确认，价格待验证 |
| 2026-07-22 | v02.1 | 创建“候选图像 API 供应商评测与阶梯报价”并行任务，记录任务 ID、询价口径、样片规模、权限边界和主任务交接顺序 | 已记录 |
| 2026-07-24 | v02.2 | 将“AI 精修实况”确认为 V1 一级能力；确认“AI 推荐 + 用户确认”、渐进式审美档案和精准引流增长闭环；新增正式产品设计规格并生成 PRD v02 | 已确认 |
| 2026-07-24 | v02.2.1 | 完成 V1 六阶段实施路线图与阶段 A“基础与 AI 精修实况纵向切片”详细计划；尚未开始业务代码 | 已规划 |
| 2026-07-26 | v02.3 | 最新决定将老照片修复纳入正式 V1，当前范围为人像精修、画质增强、路人/杂物消除和老照片修复四项工具；明确默认忠实修复、上色须主动选择并显式确认，且本次未执行供应商评测 | 已确认，覆盖此前三项工具口径 |
| 2026-07-26 | v02.4 | 功能分支为 `feat/v1-phase-a`；最新实现代码基线为 `0e77817`，Task 9 首轮文档提交为 `f10a0ac`。Node/npm 类型检查通过，严格 Vitest 为 8 文件/95 测试通过，微信开发者工具两次 compile 成功（`errors`、`warnings`、`wxml_errors` 均为空）；真实监听 API 与模拟器运行时仍待复核 | 部分验证/待运行时复核；后续修复提交继续位于该分支。以 `git log` 确认当前文档状态，`f10a0ac` 不是最终 HEAD；优先修复 9420 WebSocket 并完成六项运行时验证，API 监听受宿主 `Path/PATH` 重复键阻塞 |
| 2026-07-26 | v02.5 | 修复最终全分支审查的 C1/C2、I1–I5、M1–M3；阶段 A 输入收紧为已登记 demo，EditTrace 改为逐事件严格 payload，小程序支持终态重入、失败收敛、连续事件校验、无外部运行时模块和持久化减少动态开关；严格 Vitest 更新为 8 文件/124 测试 | 源码修复与仓库侧验证完成；3100、最终修复树微信编译、9420 交互、设备 SVG 和“关于”窗口仍待环境取证；供应商评测未执行 |
| 2026-07-27 | v02.6 | `feat/v1-phase-a` 已以纯快进方式合并至 `master`，合并基线为 `cc187d0`；规范主工作区按锁文件重建依赖后，类型检查、8 文件/124 测试及无外部模块小程序运行时验证重新通过 | 源码已集成，阶段 A 仍未验收；3100、最终修复树微信编译、9420 交互、设备 SVG 和“关于”窗口仍是进入阶段 B 或发布前门槛 |
| 2026-07-27 | v02.7 | 为新对话增加顶部快速接管区、可复制首条指令和永久证据入口；删除已随隔离工作区清理而失效的临时审查报告引用，明确当前主分支、核心代码基线、已完成项和环境验收顺序 | 仅优化交接，不改变四项 V1 范围、产品决策或阶段 A 未验收结论；供应商评测继续由独立专项处理 |
| 2026-07-27 | v02.8 | 新增真实 3100 socket 级 HTTP 冒烟并通过；在当前主分支小程序源码上重新完成两次微信编译；记录 9420 完整恢复仍失败及 Windows GUI 取证授权超时 | 3100 与微信编译门槛完成；9420 交互、设备 SVG 和“关于”窗口仍未完成，阶段 A 继续保持未验收且不可发布 |
| 2026-07-27 | v02.9 | 在 `codex/v1-phase-a-env-acceptance` 独立 worktree 修复真实微信工程根、WXSS 兼容和预览 POST `415`；全量 10 文件/129 测试、类型检查、运行时验证、微信 JS 构建及真实 IDE 编译通过；完成 9420 主链路、错误提示、减少动态持久化、水印预览与 SVG 模拟器取证 | 仅剩微信开发者工具“关于”窗口直接 GUI 版本证据和验收分支提交/集成；API 重启后内存任务不可恢复，阶段 A 仍未验收且不可发布 |
| 2026-07-28 | v03.0 | 从真实项目窗口打开“微信开发者工具 → 当前版本”，直接取得 `2.02.2607171 RC win32-x64` GUI 证据并保存截图；确认本 RC 的“关于”菜单项打开官网概览页，版本弹窗入口为“当前版本” | 阶段 A 环境证据已齐；验收分支仍待最终复核、提交与集成，在此之前不宣称阶段 A 已验收或可发布 |
| 2026-07-28 | v03.1 | 补充包含 Project-002 项目窗口和“关于 / 当前版本”菜单的组合截图；明确验收期间命令与微信导入路径必须指向独立 worktree；新增 8 个微信 JS 与临时编译输出逐文件比较的新鲜度门禁；修正当前 `master` 为 `aaeb7e2` | 独立复审的三个 Important 已处理；全量验证、真实 IDE 编译与提交前复审完成，验收分支待集成 |
| 2026-07-28 | v03.2 | `codex/v1-phase-a-env-acceptance` 通过三次纯快进集成至 `master` `05b8155`；修复 Windows CRLF/LF 造成的生成物误报，并在最终主分支重跑完整自动化门禁、真实 3100 冒烟和真实微信 IDE 编译 | 阶段 A 环境验收完成；当前 9420 WS 快速复核仍未就绪，保留 2026-07-27 直接交互证据；产品仍不可发布，阶段 B 未启动 |
| 2026-07-28 | v03.3 | 归档“审美共创互动积分”候选功能：确认平台授权样本、二选一加原因标签、低清预览权益与高清 20%–30% 抵扣边界，并冻结为行为验证通过后才考虑开发 | 不进入当前阶段 B；不得使用真实用户投稿或仅凭问卷投票启动开发 |
| 2026-07-29 | v03.4 | 启动阶段 B0 设计收敛并记录用户逐项确认：腾讯云统一托管、上海地域、原图/预览/付费结果保存期限、删除 SLA、按需微信登录、正式 AppID、COS 全私有访问；供应商当前未定但不阻塞 B0–B2 | 仅完成设计输入与交接整理，尚未完成正式规格或阶段 B 实施；不得进入 B3 真实接入或宣称可发布 |
