# V1 真实质量与成本门禁：匿名采集模板

本目录只保存匿名样本索引、盲评记录和聚合成本数据。所有样本以不可识别的 slug 关联，禁止写入图片路径、URL、OpenID、手机号、凭据或数据库连接信息。

真实图片位于经用户批准的受控来源，真实图片不入 Git；本目录也不保存、复制或链接真实图片。使用本模板前，须确认每个样本已获评估授权。

这些 fixture 仅用于可复现的 CLI 测试，不是实际市场、质量或产品发布证据。`B1-ENV` 当前未运行；任何真实质量结论、市场结论或发布决定都需要单独的受控评估与批准。

