# V1 重要照片救片体验验证记录

- 记录版本：v01
- 验证日期：2026-08-12（Asia/Shanghai）
- 验证范围：V1 重要照片救片体验 Task 1–5 的离线自动化门禁与静态边界检查
- Git 分支：`codex/v1-important-photo-rescue-experience`
- 验证基线 / HEAD：`9249377390563db59d1e6e0cb6cfe33a3e0ad8b3`
- 环境边界：未启动微信开发者工具、未连接设备、未联网、未安装依赖、未读取凭据、未调用供应商或上传数据。

## 自动化门禁

| 命令 | 结果 | 证据 |
| --- | --- | --- |
| `npm.cmd test -- --run` | PASS（离线） | 31 个测试文件通过、1 个测试文件跳过；399 项测试通过、6 项跳过、0 项失败。跳过文件为 `test/mysql-identity.integration.test.ts`，输出为 `[B1 MySQL integration] NOT ACCEPTED; skipped: MYSQL_INTEGRATION_URL is missing`。 |
| `npm.cmd run typecheck` | PASS | 4 个 workspace 的 `tsc --noEmit` 均以退出码 0 完成。 |
| `npm.cmd run verify:miniprogram-runtime` | PASS | 输出：`miniprogram runtime verification passed without external modules`。 |
| `npm.cmd run build:wechat -w @photo-ai/miniprogram` | PASS | `tsc -p tsconfig.wechat.json` 以退出码 0 完成。 |
| `git diff --check` | PASS | 退出码 0，无空白错误输出。 |

### B1-ENV 状态

- `MYSQL_INTEGRATION_URL` 未提供；6 项 MySQL 集成测试为 `NOT RUN`，不是通过结论。
- B1-ENV 仍为 `NOT RUN`，因此产品不可发布。
- 本地示例与自动化结果不代表真实图片、三系统联通或市场验证通过。

## 静态边界检查

| 命令 | 结果 | 证据 |
| --- | --- | --- |
| `rg -n '阶段 B 接入|aria-disabled|非真实用户案例|默认.*no-op|不向.*发送' 'apps/miniprogram/miniprogram' '../02-方案与设计/V1冷启动指标字典-v01-20260811.md'` | PASS | 共 16 处边界文案：阶段 B 未接入说明、禁用卡片 `aria-disabled="true"`、cases 页“示例流程 / 非真实用户案例”，以及事件白名单默认 no-op 且不持久化、不上传、不调用网络出口。 |
| `rg -n 'wx\\.reportEvent|wx\\.request|fetch\\(|taskId|openId|token|imageUrl' 'apps/miniprogram/miniprogram/services/product-events.ts'` | PASS（预期无匹配） | `rg` 返回退出码 1 且无输出；这是 ripgrep 的“无匹配”状态，未发现事件外传或识别信息字段。 |

## 微信开发者工具 / 设备人工验收

未实际启动微信开发者工具或连接设备；以下项目全部为 `NOT RUN`，不预填 `PASS`。

| 人工检查项 | 状态 | 说明 |
| --- | --- | --- |
| 首页层级：唯一可操作救片主路径，四项 V1 能力准确呈现 | NOT RUN | 需在微信开发者工具实际打开并检查。 |
| 未接入能力禁用且不可点击 | NOT RUN | 需实际点击验证。 |
| cases 页“示例流程 / 非真实用户案例”标签 | NOT RUN | 需实际渲染检查。 |
| 预览页拖动比较 | NOT RUN | 需实际手势验证。 |
| 精修详情展开 / 收起 | NOT RUN | 需实际交互验证。 |
| 减少动态偏好 | NOT RUN | 需在相应设备 / IDE 设置下验证。 |
| 失败后的恢复路径 | NOT RUN | 需注入或触发实际失败状态验证。 |

## 验收结论与限制

- 自动化范围内，页面测试、微信 TypeScript 构建和离线静态边界检查均有通过证据。
- 自动化不能替代微信 IDE / 真机、真实图片处理、三系统集成或市场验证；这些均未执行。
- 在 B1-ENV 为 `NOT RUN` 且人工验收未完成期间，产品维持不可发布状态。
