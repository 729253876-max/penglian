# PR #1 质量门禁 Task 6 本地整改验证记录

> 版本：v05
>
> 日期：2026-08-20（Asia/Shanghai）
>
> 状态：本地整改、门禁证据与最终全差异静态复审已完成；25 个终审文件由本文件所在 delivery commit 交付，未 push

## 1. 证据身份与边界

- worktree：`.worktrees/v1-faithful-portrait-rescue`
- 分支：`codex/v1-faithful-portrait-rescue`
- reviewed working-tree base HEAD：`ee5de007da612bc86835a5105d79f6a535f6c0b1`
- 所有 Fix A–F 修复及交接文档已于 2026-08-23 按 25 文件显式白名单纳入本文件所在 delivery commit；提交后以 `git rev-parse HEAD` 读取其 SHA。
- upstream/latest PR head/latest CI run head：`a1853c1281538c2ef9f55b93e0b921bc5bb7144e`。

这是本地验证记录，不是提交、push、PR 更新或远端 CI 通过证明。

## 2. 已完成：Fix A–F 的本地整改

1. **Fix A — 忠实度成功结果完整性**：成功质量结果必须完整且唯一覆盖冻结五项 `FACE_COUNT`、`IDENTITY`、`STRUCTURE`、`NON_TARGET_REGION`、`ARTIFACTS`；失败质量结果仍保留真实部分检查语义；成功夹具已修复。
2. **Fix B — workflow 契约**：以精确 `yaml@2.9.0` 结构化、fail-closed 解析 workflow，覆盖 named inline/block/folded 运行步骤、额外 trigger/job/action/run 与 job permissions。
3. **Fix C — 测试网络守卫**：当前 Vitest 进程覆盖 `net` 命名导出、`Socket`、`tls`、`http` 和 `https` 命名导出；明确不覆盖 child process、worker 或 OS 级网络边界。
4. **Fix D — live probe**：使用 pipe 和有界诊断；在 error/exit 时条件终止；返回前等待 close；cleanup timer 可取消。
5. **Fix E — 依赖治理**：`nanoid` 精确固定为 `3.3.18`；Fix E fresh clean install 的 `npm audit --json` 记录为 0 漏洞。
6. **Fix F — 小程序同步测试**：仅重型微信运行时同步测试使用局部 15 秒预算；并发全量稳定，构建正确性断言没有放宽。

## 3. 已完成：最终新鲜本地门禁

| 验证 | 结果 |
| --- | --- |
| `npm run typecheck` | PASS |
| `npm test -- --run` | 56 files passed、2 skipped；721 passed、14 skipped、0 failed |
| `npm run verify:miniprogram-runtime` | PASS |
| `npm run build:wechat -w @photo-ai/miniprogram` | PASS |
| `git diff --check` | PASS |
| `git diff --check origin/master`（含未提交工作树） | PASS |
| Fix E fresh clean-install record 的 `npm audit --json` | 0 漏洞 |

## 4. 待提交与待远端验证

| 状态 | 事项 |
| --- | --- |
| **最终静态复审完成** | 独立只读全工作树复审为 0 个本地 Critical、0 个本地 Important；Fix A–F 均 CLOSED。 |
| **stage / commit 已完成** | 2026-08-23 已分别取得授权；25 个终审文件由本文件所在 delivery commit 交付。 |
| **待授权** | push 仍必须单独取得授权。 |
| **待远端验证** | push 后才核对 delivery commit、PR head 和 CI run head 三者相同，并运行该最新 head 的远端 CI。 |
| **未发生** | 没有暂存、提交、push、合并、强推、删除 worktree/分支或外部状态写入。 |

### 4.1 Deferred Minor

- Fix B：显式增加“第三个本身已批准 action”的 mutation 测试仍 deferred。现有实现已通过精确步骤数量与顺序拒绝该行为，因此这是防回归测试覆盖 Minor，不阻止 Fix B 关闭。
- Fix D：先前审查发现的 close-wait timer 活动句柄问题已在 round 1 修复并有 resolve/reject 后 timer 数为 0 的测试；最终全差异复审未发现剩余 Fix D Minor。

## 5. 阻塞、NOT ACCEPTED 与剩余风险

- **NOT ACCEPTED**：13 个 MySQL 环境项、1 个 COS 环境项、微信 IDE/真机、真实 provider、API 重启恢复。
- PR #1 当前为 OPEN、Ready、MERGEABLE/CLEAN；`reviewDecision` 为空、`reviews` 为空。旧 `quality-gate` success 只绑定 `a1853c1`，不是本地整改的 CI。
- `master` branch protection REST 为 404。active ruleset `20883280` include `master`，含 strict `quality-gate`、0 approval、无 bypass、禁止删除/非快进及会话解决要求。
- **`BLOCKED_NEGATIVE_RULESET_TEST`**：未发现 rule evaluation/dry-run，也未获授权创建临时测试 PR/分支；不能宣称 ruleset 不可绕过。
- collaborators 只见 `729253876-max` admin；`INDEPENDENT_REVIEWER_AVAILABLE=false`。没有独立人类 reviewer 时，不建议合并。
- 同源信任域 Important 未关闭：PR 能同时修改 workflow、validator 与 tests；ruleset 仅信任同名 GitHub Actions check。结构化契约只降低语法绕过，不能建立独立信任域。禁止建议使用 `pull_request_target` checkout PR code。

## 6. 恢复顺序

1. 当前只可单独请求 push 授权。
2. push 前不得把 deferred Minor、同源信任域或 NOT ACCEPTED 边界误写为已关闭。
3. push 后取得最新同 head 的 PR/CI 证据。
4. 若需验证 ruleset，先提出无害、最小范围的负向测试方案并等待专门授权。
5. 除非用户书面接受无独立人类批准的剩余风险并选择替代信任方案，否则不建议合并。
