# V1 阶段 A 验证记录

- 验证日期：2026-07-26
- 最终集成目录：`D:\Documents\workspace\projects\Project-002-修图AI小程序`
- 原微信编译证据基线：`0e77817`（`fix: isolate miniprogram page lifecycles`）
- 集成状态：`feat/v1-phase-a` 已于 2026-07-27 纯快进合并至 `master`；
  当前 `master` 为 `aaeb7e2`（`docs: prepare fresh conversation handoff`）；
  `cc187d0` 仅是阶段 A 核心代码基线。接管时仍以实际 `git log` 为准。
- 阶段状态：最终审查中的源码问题已修复并完成仓库侧自动化验证；真实 3100
  监听、当前验收分支微信编译、9420 模拟器交互和 SVG 模拟器渲染已于
  2026-07-27 完成；开发者工具版本 GUI 证据于 2026-07-28 完成。验收分支
  尚未最终复核、提交与集成，因此阶段 A 仍未正式验收。
- Node.js：`v24.18.0`
- npm：`11.16.0`
- 微信开发者工具：真实项目窗口的“当前版本”弹窗直接显示
  `2.02.2607171 RC win32-x64`。日志版本与可执行文件
  `ProductVersion 1.03.0` 仅保留为历史旁证。

## 自动化验证

```powershell
cd "D:\Documents\workspace\projects\Project-002-修图AI小程序\.worktrees\v1-phase-a-env-acceptance\04-源文件与代码"
node --version
npm.cmd --version
npm.cmd run typecheck
npm.cmd test
npm.cmd run verify:miniprogram-runtime
```

最终修复树的实际结果：

- `node --version`：`v24.18.0`。
- `npm.cmd --version`：`11.16.0`。
- `npm.cmd run typecheck`：退出码 `0`；三个 workspace 的 `tsc --noEmit` 均完成。
- `npm.cmd test`：退出码 `0`；Vitest `8 passed` 测试文件、`124 passed`
  测试；根配置已迁移到 `test.projects`，不再输出
  `vitest.workspace.ts` 弃用警告。
- `npm.cmd run verify:miniprogram-runtime`：退出码 `0`；隔离编译产物在无
  `node_modules` 的系统临时目录加载并完成一次真实 `getTask` 调用，输出
  `miniprogram runtime verification passed without external modules`。

2026-07-27 新增真实 HTTP 冒烟后重新执行同一组验证：

- `npm.cmd run typecheck`：退出码 `0`。
- `npm.cmd test`：退出码 `0`；更新为 `9 passed` 测试文件、`125 passed`
  测试。
- `npm.cmd run verify:miniprogram-runtime`：退出码 `0`，输出仍为
  `miniprogram runtime verification passed without external modules`。

2026-07-27 阶段 A 环境验收分支最终复验：

- 分支：`codex/v1-phase-a-env-acceptance`；独立 worktree：
  `.worktrees/v1-phase-a-env-acceptance`。
- `npm.cmd run typecheck`：退出码 `0`；三个 workspace 均通过。
- `npm.cmd test`：退出码 `0`；`10 passed` 测试文件、`129 passed` 测试。
- `npm.cmd run verify:miniprogram-runtime`：退出码 `0`。
- `npm.cmd run build:wechat -w @photo-ai/miniprogram`：退出码 `0`，从
  TypeScript 生成 8 个微信运行时 `.js` 文件。
- `project-config.test.ts` 将 TypeScript 输出到系统临时目录，逐一与仓库内
  8 个 `.js` 比较；测试先通过故意加入的过期标记验证会失败，再重新生成后
  通过，证明新鲜度门禁有效。

2026-07-28 提交前新鲜复验：

- `npm.cmd run typecheck`：退出码 `0`。
- `npm.cmd test`：退出码 `0`；`10 passed` 测试文件、`129 passed` 测试。
- `npm.cmd run verify:miniprogram-runtime`：退出码 `0`。
- `npm.cmd run build:wechat -w @photo-ai/miniprogram`：退出码 `0`；随后
  `project-config.test.ts` 定向复核 `3/3` 通过。
- 全量测试前后用 `netstat -ano -p tcp` 检查，3100 均无 `LISTENING`。
- `git diff --check` 退出码 `0`；仅输出 Git 的 LF/CRLF 行尾提示。

## API 验证

计划中的真实监听 HTTP 冒烟应依次覆盖：创建人像任务 `201`、运行预览 `202`、读取任务 `200`、读取事件 `200` 且末项为 `PREVIEW_READY`、合法老照片创建 `422`。

### 2026-07-27 真实 3100 HTTP 复验

新增 `apps/api/test/http-smoke.test.ts` 作为可重复执行的 socket 级验收测试。该测试
直接使用生产 `buildApp()` 绑定 `127.0.0.1:3100`，再通过 Node 原生 `fetch`
发送真实 HTTP 请求；它不使用 Fastify `inject`。

```powershell
cd "D:\Documents\workspace\projects\Project-002-修图AI小程序\.worktrees\v1-phase-a-env-acceptance\04-源文件与代码"
npm.cmd test -- apps/api/test/http-smoke.test.ts --reporter=verbose
```

实际结果：

- 运行前以 `netstat -ano -p tcp` 确认 3100 无 `LISTENING`。
- 测试退出码 `0`，`1 passed` 测试文件、`1 passed` 测试。
- 创建人像任务返回 `201`，任务状态为 `AWAITING_CONFIRMATION`。
- 运行预览返回 `202`，任务状态为 `SUCCEEDED`，预览 URL 为登记的示例水印图。
- 读取任务返回 `200`，终态为 `SUCCEEDED`。
- 读取事件返回 `200`，共 9 条，`nextSequence` 为 `9`，末项为
  `PREVIEW_READY`。
- 合法老照片契约在阶段 A API 边界返回 `422`，
  `code = STAGE_A_UNSUPPORTED_TOOL`。
- 测试关闭 Fastify 后 3100 无 `LISTENING`；仅出现正常 TCP `TIME_WAIT`。

排障记录：本轮最早一次受控后台启动因 WMI 进程树读取被拒绝，留下 Node
PID `10484` 占用 3100；`Get-NetTCPConnection` 在当前权限下漏报该监听，
`netstat -ano -p tcp` 成功定位。确认该 PID 的启动时间与本轮命令一致后仅清理
该 PID，再执行上述测试通过。后续端口前后置检查统一以 `netstat` 为准。

后续受控启动和 HTTP 冒烟必须从与自动化验证相同的源码目录执行：

```powershell
cd "D:\Documents\workspace\projects\Project-002-修图AI小程序\.worktrees\v1-phase-a-env-acceptance\04-源文件与代码"
Start-Process -FilePath npm.cmd -ArgumentList 'run', 'dev:api' -WorkingDirectory (Get-Location) -WindowStyle Hidden -PassThru
```

上面的命令仍可用于人工启动；自动化复验优先使用 socket 级测试命令。

### 2026-07-26 历史阻塞记录

- 启动前以 `Get-NetTCPConnection -LocalPort 3100 -State Listen` 检查，未发现未知监听。
- 以 `Start-Process -WindowStyle Hidden` 启动 `npm.cmd run dev:api`，仅记录本次 PID `21588`；连续轮询端口 15 秒，没有发现 3100 监听。
- `finally` 中只停止 PID `21588`；清理输出为 `cleanup_tree_pids:[21588]`、`stopped_pids:[21588]`、`remaining_own_pids:[]`。未停止任何未知 Node/npm 进程。
- 后续为取得启动诊断而增加标准输出/错误重定向时，`Start-Process` 在创建进程前报错：`Path/PATH` 重复键导致字典冲突。该宿主环境问题阻止后续启动诊断；按同一失败操作上限停止重试。
- 因服务没有实际监听，本次没有发送或记录 `201/202/200/422` 的 HTTP 请求结果。

该历史阻塞已由 2026-07-27 的真实 socket 监听与 HTTP 复验解除。Task 6 的
Fastify `inject` 证据仍仅作为路由级证据保留。

## 微信开发者工具

### 编译

Task 8 于 2026-07-26 在 `0e77817` 基线两次编译成功：工程目录为
`04-源文件与代码/apps/miniprogram`，AppID 为 `touristappid`，`errors`、
`warnings`、`wxml_errors` 均为空。最终审查修复修改了 TypeScript、WXML 和
WXSS；本记录没有把旧基线编译证据冒充为修复后编译证据。

2026-07-27 在干净 `master`、HEAD `aaeb7e2` 的当前小程序源码上重新编译两次：

- `compiled: true`。
- `errors: []`。
- `wxml_errors: []`。
- 两条 warning 均来自开发者工具 Node 的 `punycode` 弃用提示，不是小程序
  源码错误。
- 工程绝对路径为
  `D:\Documents\workspace\projects\Project-002-修图AI小程序\04-源文件与代码\apps\miniprogram`。
- `automator_reconnected: false`；该字段不影响编译通过结论，但 9420 交互仍需
  独立验收。

环境验收期间真实 IDE 将父工程的页面错误解析为父目录下的 `pages/...`，证明
该 RC 版本不能可靠处理原父工程的 `miniprogramRoot`。修复后微信实际导入目录
改为：

`D:\Documents\workspace\projects\Project-002-修图AI小程序\.worktrees\v1-phase-a-env-acceptance\04-源文件与代码\apps\miniprogram\miniprogram`

嵌套运行时工程使用 `miniprogramRoot: "./"`，并以 `beforeCompile` 执行
`npm --prefix .. run build:wechat`。同时移除了全局 `app.wxss` 中微信 WXSS
不支持的 `@media (prefers-reduced-motion: reduce)`。当前验收分支最终复编译：

- `compiled: true`，`errors: []`，`wxml_errors: []`。
- `Using AppID: touristappid`。
- `automator_reconnected: true`，`automator_verified: true`。
- 编译后 `page_data.path === "pages/live/index"`。

2026-07-28 完成版本 GUI 取证并从项目列表导入同一运行时工程后，再次对该绝对
路径执行真实 IDE 编译：

- `compiled: true`，`errors: []`，`wxml_errors: []`。
- `Using AppID: touristappid`。
- 两条 warning 仍为开发者工具 Node 的 `punycode` 弃用提示。
- 本次 `automator_reconnected: false`，因此只作为新鲜编译证据；9420 交互
  继续以 2026-07-27 已完成的直接运行时证据为准，不把该字段写成新的交互结论。

提交前对明确的 worktree 运行时工程再次编译，结果仍为 `compiled: true`、
`errors: []`、`wxml_errors: []`、`Using AppID: touristappid`；两条 warning
仍仅为工具自身 `punycode` 弃用提示。本次 `automator_reconnected: false`。

### 模拟器运行时交互

2026-07-27 修复后 9420 返回 `tcp_ready: true`、`ws_ready: true`、
`verified: true`，以下项目已取得直接运行时证据：

- [x] 首页加载并进入方案确认页。
- [x] 示例任务创建成功。
- [x] AI 精修实况按 sequence 1–9 展示，末项为 `PREVIEW_READY`。
- [x] 重新进入同一实况页仍为 9 条事件，没有重复渲染。
- [x] 水印预览页打开。
- [x] API 停止时显示
  `精修任务暂时无法继续，请检查本地 API 后重试。`，并存在文本为
  `重新获取进度` 的 `.retry` 按钮。
- [x] “减少动态效果”开关即时生效并持久化；
  `photo-ai:reduce-motion = true`，重入同一任务后 `reduceMotion` 仍为 `true`。
- [x] 示例 SVG 在微信模拟器正常渲染，人工查看的证据截图为：
  - `06-复盘与踩坑/阶段A-微信模拟器-原图SVG-20260727.png`
  - `06-复盘与踩坑/阶段A-微信模拟器-精修后SVG-20260727.png`

成功任务 ID 为 `7cc529fe-1894-44bc-be6d-af6a03eb01d9`。最终复验的
`page_data` 仍显示 `status: "SUCCEEDED"`、`ready: true`、`failed: false`、
9 条连续事件和 `reduceMotion: true`。

真实交互还暴露了 `runPreview()` 发送无 body POST 时 Fastify 返回 `415`。
修复先增加失败测试，再把请求改为 `data: {}`；定向 `api.test.ts` 为
`22/22` 通过，全量测试亦通过。

API 停止场景只验证了错误提示与重试入口。API 仓储为阶段 A 内存态，重启后
旧任务已经丢失，所以同一任务不能恢复；不得把这一结果写成“API 重启后恢复
成功”。持久化恢复属于后续阶段能力，不在本轮补做。

2026-07-28 提交前编译后的快速连接核验返回 `tcp_ready: true`、
`ws_ready: false`、`verified: false`；随后一次 `page_data` 在 120 秒后超时，
因此停止重复。本次没有形成新的 9420 功能性结论，也不覆盖 2026-07-27 已完成
的 9 事件主链路、失败提示、减少动态持久化和水印预览直接证据。

以下是已解除的历史阻塞，不代表当前状态：Task 8 两次 automator `start`
均为 TCP 9420 已就绪而 WebSocket 未就绪；随后 `page_data` 返回
`Connection not ready: health check failed after connect`。

2026-07-27 当前环境的新证据：快速恢复与一次完整恢复均失败。`start` 返回
“CLI auto 返回成功但端口 9420 未监听”，`page_data` 返回
`Failed connecting to ws://localhost:9420`；完整恢复中的重新编译仍成功。
达到同一失败操作上限后停止重复。

补充旁证与边界：

- 9222 CDP 采集 5 秒返回 0 errors、0 warnings；最终 SVG 结论以两张模拟器
  截图及人工查看为准，不再用 CDP 旁证代替。
- 2026-07-28 通过项目列表“导入”打开现有运行时工程后，真实项目窗口顶部
  应用菜单可访问。“关于”菜单项会打开微信开发者工具官网概览页；同一菜单的
  “当前版本”会打开本机版本弹窗。
- “当前版本”弹窗直接显示
  `当前版本: 2.02.2607171 RC win32-x64`。GUI 证据截图为：
  `06-复盘与踩坑/阶段A-微信开发者工具-当前版本-20260728.png`。
- 完整项目窗口与菜单入口的组合证据为：
  `06-复盘与踩坑/阶段A-微信开发者工具-版本入口-20260728.jpg`；图中同时
  显示 Project-002 首页模拟器及“关于 / 当前版本”菜单，已人工复核。
- 该直接 GUI 证据取代此前仅有日志 `2.02.2607171` 和可执行文件
  `ProductVersion 1.03.0` 的旁证状态。

## 限制与解除阻塞步骤

- 阶段 A 只使用示例资产和本地模拟服务；没有真实上传、审核、支付、积分、审美档案、供应商接入或长期持久化。
- 阶段 B 的三项 UI 工具尚未接入；老照片修复的上色请求与显式确认仅保留为共享契约。
- 阶段 A 仅登记 `demo-portrait-001 + NATURAL + brightness 0 + warmth 0 +
  naturalness 80`；任何其他资产、方向或参数在仓储写入前拒绝。
- 小程序无需微信 npm 构建；仓库验证的事实仅为运行时 service 不依赖外部
  模块，不能替代微信开发者工具编译或模拟器验证。
- API 真实监听阻塞已解除；后续优先运行 `apps/api/test/http-smoke.test.ts`，
  并使用 `netstat` 做 3100 前后置监听检查。
- 9420 模拟器交互与 SVG 渲染阻塞已经解除。
- 微信开发者工具版本直接 GUI 证据已经完成；本 RC 的入口名称为“当前版本”，
  “关于”菜单项会打开官网概览页。

## 阶段 B 进入条件

真实监听 API 冒烟、当前验收分支微信编译、9420 小程序运行时交互和模拟器
SVG、微信开发者工具版本直接 GUI 证据均已完成。在进入阶段 B 前，仍必须完成
验收分支最终复核、提交与集成；供应商评测继续由独立专项处理，本记录不写入
其结果、名称或价格。
