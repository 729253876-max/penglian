# PR #1 质量门禁自检与暂停记录（Task 2 复核）

> 版本：v04  
> 日期：2026-08-15（Asia/Shanghai）  
> 状态：Task 2（多行 workflow 运行命令多层级检查）已完成；等待用户确认是否继续 Task 4/5 与合并授权

## 现场核验（本次）

- 规范项目：`D:\Documents\workspace\projects\Project-002-修图AI小程序`
- worktree：`D:\Documents\workspace\projects\Project-002-修图AI小程序\.worktrees\v1-faithful-portrait-rescue`
- 分支：`codex/v1-faithful-portrait-rescue`
- `HEAD`：`a5503aabae79ec9ef66a6caeb2854a631cf22e80`
- `upstream`：`origin/codex/v1-faithful-portrait-rescue`（`a1853c1281538c2ef9f55b93e0b921bc5bb7144e`）
- PR #1：`open`、`Ready`、`mergeStateStatus=CLEAN`、`mergeable=MERGEABLE`
- PR head SHA（来自 GitHub）：`a1853c1281538c2ef9f55b93e0b921bc5bb7144e`（与上一次记录一致）
- 近一次 quality-gate：`SUCCESS`
- `master` branch protection：`404`（未启用）
- repository rulesets：`id=20883280` 已激活

## Task 2 执行记录（按计划）

1. `packages/ci-contract/src/workflow-contract.ts`
   - 新增 `run` 命令解析 metadata（`inline/block/folded`）。
   - `FORBIDDEN_SECRET` 增加全文扫描：
     - 任意 `${{ secrets.X }}`；
     - 或敏感变量声明 `MYSQL_INTEGRATION_URL/COS_SECRET/STS_SECRET/MODEL_API_KEY`。
   - 远程写命令检测按 run 风格区分：
     - inline 保持 `FORBIDDEN_REMOTE_OPERATION`
     - block scalar 增加 `FORBIDDEN_REMOTE_OPERATION_BLOCK`
     - folded scalar 增加 `FORBIDDEN_REMOTE_OPERATION_FOLDED`
2. 保持现有 `WorkflowContractCode` 与校验链路兼容；仅在失败用例范围内新增更细分类码。

## 验证结果

- `npm.cmd test -w @photo-ai/ci-contract -- --run`：`20` 个测试通过
- `npm run typecheck`：通过
- `npm test -- --run`：`56` 文件，`677` 用例通过（`14` skipped）
- `npm.cmd test -- --run` 全量日志中仅记录两项预期未接收项：`MySQL` 集成 skipped（`NOT ACCEPTED`）

## 风险与自检结论

- 本次修正未涉及业务功能；未引入外部凭据/模型/网络写操作。
- 仍需注意：
  - 当前 `INDEPENDENT_REVIEWER_AVAILABLE` 仍为 `false`（当前 PR 无 reviewDecision）。
  - `master` 未启用 branch protection；ruleset 仍是外部状态开关。
  - 依赖告警（`nanoid GHSA-2v37-7h3g-55p8`）仍未处理。

## 下一步建议

1. 用户确认后继续：
   - Task 4（Node/npm 精确化与 live probe）
   - Task 5（高危告警决策）
2. 本地工作树保持不再自动合并/强推/删除；提交前请先审批后续外部写操作（ruleset/PR 操作）。  
