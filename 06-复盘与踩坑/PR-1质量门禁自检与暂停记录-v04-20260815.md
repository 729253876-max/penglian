# PR #1 质量门禁自检与暂停记录（Task 2 + Task 3 复核）

> 版本：v04（复核更新）  
> 日期：2026-08-15（Asia/Shanghai）  
> 状态：Task 2 已完成；Task 3（测试网络 localhost-only 门禁）验证通过；等待用户确认是否继续 Task 5 与外部治理动作

## 现场核验（本次）

- 规范项目：`D:\Documents\workspace\projects\Project-002-修图AI小程序`
- worktree：`D:\Documents\workspace\projects\Project-002-修图AI小程序\.worktrees\v1-faithful-portrait-rescue`
- 分支：`codex/v1-faithful-portrait-rescue`
- `HEAD`：`feee006`（当前尚有未提交 Task 3 本地改动）
- `upstream`：`origin/codex/v1-faithful-portrait-rescue`（`a1853c1281538c2ef9f55b93e0b921bc5bb7144e`）
- PR #1：`open`、`Ready`、`mergeStateStatus=CLEAN`、`mergeable=MERGEABLE`
- PR head SHA（来自 GitHub）：`a1853c1281538c2ef9f55b93e0b921bc5bb7144e`
- `master` branch protection：`404`（未启用）  
- repository rulesets：`id=20883280` 已激活
- `INDEPENDENT_REVIEWER_AVAILABLE`：`false`（当前无独立审查决策）

## Task 2 执行记录（按计划）

1. `packages/ci-contract/src/workflow-contract.ts`
   - 新增 `run` 命令解析 metadata（`inline/block/folded`）。
   - `FORBIDDEN_SECRET` 增加全文扫描：
     - 任意 `${{ secrets.X }}`；
     - 或敏感变量声明 `MYSQL_INTEGRATION_URL/COS_SECRET/STS_SECRET/MODEL_API_KEY`。
   - 远程写命令检测按 run 风格区分：
     - inline 保持 `FORBIDDEN_REMOTE_OPERATION`
     - block scalar 增加 `FORBIDDEN_REMOTE_OPERATION_BLOCK`
     - folded scalar 增加 `FORBIDDEN_REMOTE_OPERATION_FOLDED`
2. 保持现有 `WorkflowContractCode` 与校验链路兼容；仅在失败用例范围内新增更细分类码。

## Task 3 执行记录

- 新增独立测试包：`04-源文件与代码/packages/test-network-policy`
  - `package.json`
  - `tsconfig.json`
  - `vitest.config.ts`
  - `vitest.network-policy.ts`（`TEST_NETWORK_EGRESS_FORBIDDEN` 规则）
  - `test/network-policy.test.ts`
- 为测试进程接入局部网络守卫（setupFiles）：
  - `04-源文件与代码/apps/api/vitest.config.ts`
  - `04-源文件与代码/apps/miniprogram/vitest.config.ts`
  - `04-源文件与代码/packages/ci-contract/vitest.config.ts`
  - `04-源文件与代码/packages/contracts/vitest.config.ts`
  - `04-源文件与代码/packages/market-validation/vitest.config.ts`
- 关键校验覆盖：
  - `fetch` 对 `https://example.invalid` 拒绝；
  - `net.createConnection({host:"example.invalid"})` 拒绝；
  - `https.request({host:"example.invalid"})` 拒绝；
  - `http://127.0.0.1:*` 本地访问放行。

## 验证结果

- `npm test -w @photo-ai/test-network-policy -- --run`：通过（`1` 文件 / `4` 测试）
- `npm run typecheck -w @photo-ai/test-network-policy`：通过
- 抽样项目命令验证通过：
  - `npm test -w @photo-ai/api -- --run test/config.test.ts`
  - `npm test -w @photo-ai/miniprogram -- --run test/pages.test.ts`
  - `npm test -w @photo-ai/contracts -- --run test/upload.test.ts`
  - `npm test -w @photo-ai/market-validation -- --run test/gate.test.ts`
- 全量验证：
  - `npm run typecheck`：通过
  - `npm test -- --run`：`56` 测试包、`677` passed、`14` skipped、`0` failed
- `npm test -- --run` 仍保留两类预期未接收项：
  - `MYSQL` 集成缺失环境跳过（`NOT ACCEPTED`）
  - COS 相关集成缺失环境保持 `NOT ACCEPTED`

## 关键风险与边界

- 本次修正未引入业务功能变更；未接入真实服务、凭据、模型、部署或上传路径。
- `ruleset` 与 `merge` 仍为外部状态开关，不能替代强制 required 保护。
- `nanoid` 高危告警（`nanoid@3.3.16`）仍未处理。
- 缺少独立 reviewer，PR 当前无法满足“独立全差异复审”正式闭环。

## 下一步建议

1. 用户确认后继续：
   - Task 5：高危依赖治理决策（需外部授权后查证与处置）
2. 本地工作树继续不做：自动合并、强推、删除 worktree/分支、真实服务接入。
3. 全量修订完成后再次提交暂停版交接与复盘，再继续下一轮合并前治理。  
