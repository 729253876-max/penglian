# V1 阶段 B1 验证记录 v01

- 记录日期：2026-08-11（Asia/Shanghai）
- 分支：`codex/v1-b1-identity-consent`
- 原单门禁 Task 7 起始提交：`cc48c18a6d5b6f483d5525f73c91fd4c13ad35f0`
- 双门禁文档迁移基线：`d2a10f3ec65ed2109a966dcc1d29287e93314608`
- B1-CODE：**PASS / ACCEPTED**
- B1-ENV：**NOT RUN / NOT ACCEPTED**
- 发布结论：**B1 未全部通过，不可据此接入真实用户、真实图片或发布。B1-ENV 不
  阻塞 B2 本地代码、模拟器和契约测试，但不授权任何云资源或外部动作。**

## 1. 本轮边界

本轮只完成无需外部资源的测试代码、说明和仓库自动化门禁。环境预检确认没有
`mysql`、`mysqld`、Docker、MySQL service、可用的 3306 监听或
`MYSQL_INTEGRATION_URL`，也没有 `WECHAT_APP_SECRET`。因此没有尝试连接、安装或
下载 MySQL，没有用正式 AppID 执行 IDE、preview 或真机验收，没有扫码、upload
或云资源动作。仓库写入正式 AppID 只是配置事实，不是环境证据。

## 2. Fresh 仓库门禁（2026-08-09）

所有命令均从原单门禁 Task 7 指定 worktree 的 `04-源文件与代码` 运行。

| 门禁 | 最新结果 | 可支持的结论 |
| --- | --- | --- |
| `npm.cmd run typecheck` | 外层 npm 命令 exit 1；底层 API workspace/tsc lifecycle code 2；唯一诊断为未跟踪 MySQL 草稿缺少必填 `accessTokenLifetimeMilliseconds` | 全 workspace 类型检查未通过；不能以其余 workspace 成功覆盖 |
| `npm.cmd test -- --run` | exit 0；22 个测试文件通过，1 个文件 skipped；321 个测试通过，6 个测试 skipped | 非 MySQL 自动化测试通过；MySQL 6 项未执行 |
| `npm.cmd run verify:miniprogram-runtime` | exit 0；`miniprogram runtime verification passed without external modules` | 仓库侧隔离 runtime 验证通过 |
| `npm.cmd run build:wechat -w @photo-ai/miniprogram` | exit 0；`tsc -p tsconfig.wechat.json` | 仓库侧微信 TypeScript 构建通过 |

原单门禁 Task 7 初稿曾修复 Promise 类型谓词后取得旧基线的 typecheck 通过；随后双门禁
Task 1 把访问 TTL 改成内部必填参数，而未跟踪 MySQL 草稿按保护规则一直未被修改。
因此当前 fresh typecheck 的真实结果是外层 exit 1，不能继续沿用旧的 exit 0；
输出中的 code 2 属于底层 API workspace/tsc lifecycle。该草稿
必须由 Task 6 在获准运行真实 MySQL 前按生产契约补齐，而不是放宽生产类型规避。

## 3. MySQL 8 集成测试状态

新增的 `apps/api/test/mysql-identity.integration.test.ts` 使用真实 `mysql2` pool、
真实 `IdentityService`、真实 `MySqlIdentityRepository` 和仓库内
`migrations/001_identity.sql`，没有用 fake 代替数据库验收。它在获得资源后会串行
验证：

1. 生产迁移可在 MySQL 8 上重复执行；
2. 同一 OpenID 并发及重复 login 只产生一个用户和一条 identity binding；
3. 同一 refresh token 的两个并发请求恰有一个成功；
4. 第六台设备登录后最旧 session 被撤销；
5. 关闭并重建应用连接池后，未撤销 session 仍可 refresh；
6. 已撤销 session 在应用连接池重建后仍被拒绝。

安全边界：只有数据库名匹配 `photo_ai_b1_test` 或安全后缀，且
`MYSQL_INTEGRATION_ALLOW=1` 时才会连接。清理仅对已验证专用库内四张身份表执行
`DELETE`；不 `DROP DATABASE`，不打印 URL 或密码。危险数据库名会在连接、迁移和
清理之前拒绝。

本轮聚焦命令：

```text
npm.cmd test -- apps/api/test/mysql-identity.integration.test.ts --run --reporter=verbose
```

从正确 worktree 运行的真实结果为 exit 0，但 **1 个测试文件 skipped、6 个测试
skipped、0 个 MySQL 测试执行**；stderr 明确输出：

```text
[B1 MySQL integration] NOT ACCEPTED; skipped: MYSQL_INTEGRATION_URL is missing
```

因此 MySQL concurrency、迁移和应用连接池重建验收全部是 **NOT RUN / NOT ACCEPTED**，
不能把命令 exit 0 或 skipped 写成 PASS。

应用连接池关闭与重建不是 MySQL 实例重启；本轮没有验证数据库实例重启、故障
切换、备份或灾难恢复。readiness 的 `1500ms` 只是 mysql2 query inactivity
timeout，也不能据此声称端到端 readiness 在 1500ms 内必然返回。

另以无凭据、数据库名不符合安全前缀的本地占位 URL 和显式 allow 开关运行聚焦
文件，结果为 exit 1、0 tests，并在测试收集阶段返回
`MYSQL_INTEGRATION_REFUSED_UNSAFE_DATABASE`。该证据只证明危险库名 guard 在建连前
拒绝，不证明任何 MySQL 业务场景通过。

另有一次从规范项目主工作树而非本 Task worktree 误运行同一过滤命令，Vitest 因
找不到新文件以 exit 1 报告 `No test files found`。纠正运行目录后才取得上述明确
skip 证据；这次失败不代表 MySQL 被执行。

## 4. 微信 IDE、正式 AppID 与三系统状态

- 原 B1 Task 6 控制器记录只证明 `touristappid` 的 IDE compile 层；automator
  `page_data` 因工具错误未完成，不能写成页面交互通过。
- 双门禁 Task 3 已把两个工程配置统一写入正式 AppID，并为 local、acceptance、
  production 增加构建期 API origin 配置。该事实不等于正式 AppID IDE compile、
  preview、受控 API 连通或设备登录通过。
- Task 4 已把错误节点改为 `aria-role="alert"`，但只有实现者与 reviewer 的标记
  自审；TypeScript build 不解析 WXML。微信 IDE 接受度和真机无障碍语义均未验证。
- 正式 AppID / AppSecret 动作未获本轮授权，`WECHAT_APP_SECRET` 不存在；正式登录、
  refresh、第二设备、当前设备退出、全部设备退出和账户删除入口均未执行。第六设备
  淘汰由真实 MySQL 测试承担，不需要六台物理手机。

| 系统 | 设备/系统版本 | 微信版本 | 原生/兼容 | 登录 | 重启恢复 | refresh | 当前退出 | 删除入口 | 证据 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Android | 未提供 | 未提供 | 原生 | NOT RUN | NOT RUN | NOT RUN | NOT RUN | NOT RUN | 无 | NOT RUN |
| iOS | 未提供 | 未提供 | 原生 | NOT RUN | NOT RUN | NOT RUN | NOT RUN | NOT RUN | 无 | NOT RUN |
| HarmonyOS | 未提供 | 未提供 | 未确认 | NOT RUN | NOT RUN | NOT RUN | NOT RUN | NOT RUN | 无 | NOT RUN |

HarmonyOS 原生微信与 Android 兼容层必须作为不同证据分别记录；当前“未确认”不能
借用 Android 结果，也不能合并写成“鸿蒙通过”。

## 5. B1 双门禁判定

### 5.1 B1-CODE Completion Gate

| 必需证据 | 当前状态 |
| --- | --- |
| 仓库自动化门禁 | PASS：23 文件、334 测试通过；typecheck、runtime、微信 TypeScript build 与 diff check 通过 |
| MySQL 8 迁移、并发与应用连接池重建 | PASS：1 文件、6/6 通过、0 skipped |
| B1 最终全分支审查 | PASS：`6908ac1..109bf62` 无遗留 Critical/Important；两项 consent Important 已修复 |

**B1-CODE：PASS / ACCEPTED。** 这只允许进入代码集成决策，不代表 B1-ENV、真实
用户、真实图片或发布通过。

### 5.2 B1-ENV Completion Gate

| 必需证据 | 当前状态 |
| --- | --- |
| 正式 AppID IDE compile、preview 与受控 API | NOT RUN / NOT ACCEPTED |
| Android | NOT RUN / NOT ACCEPTED |
| iOS | NOT RUN / NOT ACCEPTED |
| HarmonyOS（运行形态分开记录） | NOT RUN / NOT ACCEPTED |

**B1-ENV：NOT RUN / NOT ACCEPTED。** 三系统正式 AppID 证据全部通过后，才允许
真实用户身份、真实图片和发布。该门禁不阻塞 B2 本地代码、模拟器和契约测试。

## 6. 两道门禁各自的继续条件

### 6.1 Task 6 / B1-CODE

Task 6 只依赖以下 MySQL 前置：

1. 用户明确批准可访问的 MySQL 8 专用测试库及破坏性集成测试；数据库名使用
   `photo_ai_b1_test` 或安全后缀。
2. 测试账号连接材料通过受控环境注入，不把 URL、密码写入仓库、聊天或日志。
3. 当前测试进程显式设置 `MYSQL_INTEGRATION_ALLOW=1`。

任一 MySQL 前置未齐时，Task 6 状态为 `NEEDS_CONTEXT`，`B1-CODE` 保持 `PARTIAL`。
三系统设备、正式 AppID/AppSecret 或微信 IDE 前置不属于 Task 6，不得阻塞真实
MySQL 测试和 B1-CODE 判定。

## 7. Task 6：真实 MySQL 8 与 fresh 自动化证据（2026-08-11）

本节为最新 B1-CODE 自动化证据，取代本记录中“typecheck 失败 / MySQL skipped”的历史快照；它不替代独立最终审查，也不改变 B1-ENV 的状态。

- 工作树：`D:\Documents\workspace\projects\Project-002-修图AI小程序\.worktrees\v1-b1-identity-consent`
- 代码目录：`04-源文件与代码`
- 最终代码/测试基线：`109bf62`（文档提交后 HEAD 会前进）
- 临时实例代号：`Project-002-local-mysql8-ec9bd158`；仅以该非秘密代号记录，未写入 URL、地址、端口、账户、密码或数据库实值。

### 7.1 TDD：MySQL 测试夹具与 deadlock 重试

1. RED：全量 typecheck 准确复现 `TS2345`，原因是未跟踪的真实 MySQL 集成测试夹具缺少生产契约必填的 `accessTokenLifetimeMilliseconds`。
2. GREEN：只为测试夹具补入固定两小时 TTL，未放宽生产 `IdentityConfig`。
3. 真实 MySQL RED：同 OpenID 并发登录返回 MySQL `ER_LOCK_DEADLOCK`（errno 1213）；其余 5 项通过。根因是不存在 identity 的 `SELECT ... FOR UPDATE` 与随后唯一 binding 插入的并发锁顺序。
4. 单元 RED：为 `withTransaction` 新增三项行为测试，确认当时没有重试。
5. GREEN：`withTransaction` 只在错误同时具有 `code=ER_LOCK_DEADLOCK` 和 `errno=1213` 时重试，最多三次；每次重试前完成 rollback/release，并以新的连接重新 begin。非 deadlock 不重试，耗尽后抛回原错误。聚焦单元测试为 7/7 通过。

### 7.2 真实 MySQL 8 结果与清理边界

受控 PowerShell 包装在每个需要数据库的进程中从 DPAPI 临时加载 URL，并设置 `MYSQL_INTEGRATION_ALLOW=1`；`finally` 删除两个环境变量、`ZeroFreeBSTR`、释放 `SecureString` 并清空明文。测试 guard 已确认 MySQL 主版本为 8，且仅接受授权开关与专用测试库命名。聚焦命令：

```text
npm.cmd test -- apps/api/test/mysql-identity.integration.test.ts --run --reporter=verbose
```

结果：1 个测试文件，6/6 PASS，0 skipped。生产迁移幂等、并发登录、并发 refresh、第六设备淘汰、连接池重建后活跃会话和撤销会话均通过。测试 before/after hooks 未报告清理失败；清理只针对 guard 已通过的专用库中 `sessions`、`consents`、`identity_bindings`、`users` 四张身份表执行 DELETE，不执行 DROP DATABASE。Task 6 未停止实例、未删除临时目录，也未删除 DPAPI 文件。

### 7.3 fresh 门禁

| 命令 | 最新结果 |
| --- | --- |
| `npm.cmd run typecheck` | exit 0 |
| 受控 DPAPI 环境下 `npm.cmd test -- --run` | exit 0；23 文件、334 测试通过；MySQL 文件 6/6、0 skipped |
| `npm.cmd run verify:miniprogram-runtime` | exit 0；`miniprogram runtime verification passed without external modules` |
| `npm.cmd run build:wechat -w @photo-ai/miniprogram` | exit 0 |
| `git diff --check` | exit 0 |

### 7.4 独立最终审查

controller 审查 Task 6 五文件提交及固定范围 `6908ac1..HEAD` 后，确认 MySQL deadlock 重试仅匹配 `ER_LOCK_DEADLOCK`/1213、最多三次、每次重新取连接，且危险库名 guard、显式授权开关、清理范围和秘密边界未被放宽。审查同时发现并以 TDD 修复两项 Important：

1. 后端此前允许 `metadataRemoval=false` 登录并签发会话；
2. 后端此前接受任意客户端自报 `policyVersion`，不能证明用户同意当前批准政策。

修复后，共享登录契约与 `IdentityService` 双层要求 `metadataRemoval=true` 且政策版本精确为 `2026-08-02`；反例在修复前分别得到 API 500/服务签发 token 的 RED，修复后聚焦 116/116 与完整 334/334 均通过。最终 HEAD 首轮全量复核曾因并行负载使固定 1 秒的 server import 子进程探针在 1.06 秒时超时，其他 333 项和 MySQL 6 项通过；根因是测试调度预算，不是 server 启动监听。探针上限改为 5 秒后仍要求子进程自行 exit 0 并输出 `IMPORT_OK`，聚焦文件连续三次通过。最终审查没有遗留 Critical/Important。已知 Minor：runtime config 生成器的外层 cleanup 失败仍可能遮蔽原始替换错误；不影响本次配置完整性和 B1-CODE 判定，留待诊断加固。

结论：`B1-CODE` 为 `PASS / ACCEPTED`；`B1-ENV` 继续为 `NOT RUN / NOT ACCEPTED`。代码通过不等于微信 IDE、WXML、正式登录或 Android/iOS/HarmonyOS 真机通过，产品仍不可发布。

## 8. Task 7 / B1-ENV

Task 7 另行依赖以下微信环境前置：

1. 用户批准正式 AppID 测试、扫码/登录及可能的 API 入口费用；AppSecret 以不落盘
   方式注入受控后端。
2. 手机可访问的受控非生产 API、微信开发者工具与正式工程权限。
3. Android、iOS、HarmonyOS 设备和专用测试账号；HarmonyOS 原生微信与 Android
   兼容层分别留证。
4. 微信 IDE 的 WXML 编译和页面交互证据；不能用 TypeScript build、仓库 AppID
   配置或历史 touristappid compile 代替。

任一环境前置未齐时，Task 7 不启动，`B1-ENV` 保持 `NOT RUN`；已确认外部能力缺失
时记为 `BLOCKED_EXTERNAL`。这不改变已经通过的 B1-CODE；B1-ENV 继续保持
`NOT RUN / NOT ACCEPTED`。
