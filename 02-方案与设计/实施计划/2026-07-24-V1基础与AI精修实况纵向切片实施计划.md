# V1 基础与 AI 精修实况纵向切片 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立可运行的原生微信小程序与 Node.js API 纵向切片，让用户使用模拟图片资产创建人像精修任务、查看真实结构化 AI 精修实况事件并进入水印预览页。

**Architecture:** 使用 npm workspaces 管理小程序、API 和共享契约。API 通过领域状态机、仓储接口、`ImageProvider` 适配器和 `EditTrace` 事件策略隔离业务；第一阶段使用内存仓储与确定性模拟供应商，先验证契约、真实事件和前端体验，不提前耦合真实供应商与资金系统。

**Tech Stack:** Node.js `v24.18.0 LTS`、npm workspaces、TypeScript、Fastify 5、Zod 4、Vitest 3、原生微信小程序 TypeScript/WXML/WXSS。

## Global Constraints

- 所有文件必须位于 `D:\Documents\workspace\projects\Project-002-修图AI小程序`。
- 源码唯一目录为 `04-源文件与代码`。
- Windows PowerShell 使用 `npm.cmd`，不直接使用 `npm`。
- V1 只包含人像精修、画质增强、路人/杂物消除。
- 第一阶段只实现人像精修模拟纵向切片，另外两项保留共享契约，不实现页面。
- 小程序不得直接调用供应商或包含供应商密钥。
- AI 精修实况只渲染服务端真实事件，不使用计时器编造事件。
- 价格、积分、输出规格、供应商和实验不得写死。
- 第一阶段不接入微信支付、真实用户图片、真实供应商和长期审美档案。
- 项目当前不是有效 Git 仓库；执行前须先完成仓库初始化授权，之后每个任务按计划提交。

---

## File Structure

```text
04-源文件与代码/
├─ package.json
├─ tsconfig.base.json
├─ vitest.workspace.ts
├─ .gitignore
├─ apps/
│  ├─ api/
│  │  ├─ package.json
│  │  ├─ tsconfig.json
│  │  ├─ src/
│  │  │  ├─ app.ts
│  │  │  ├─ server.ts
│  │  │  ├─ domain/
│  │  │  │  ├─ task-machine.ts
│  │  │  │  └─ edit-trace-policy.ts
│  │  │  ├─ application/
│  │  │  │  └─ task-service.ts
│  │  │  ├─ infrastructure/
│  │  │  │  ├─ in-memory-task-repository.ts
│  │  │  │  └─ mock-image-provider.ts
│  │  │  └─ routes/
│  │  │     └─ tasks.ts
│  │  └─ test/
│  │     ├─ task-machine.test.ts
│  │     ├─ edit-trace-policy.test.ts
│  │     ├─ task-service.test.ts
│  │     └─ tasks-api.test.ts
│  └─ miniprogram/
│     ├─ package.json
│     ├─ project.config.json
│     ├─ tsconfig.json
│     └─ miniprogram/
│        ├─ app.json
│        ├─ app.ts
│        ├─ app.wxss
│        ├─ services/api.ts
│        ├─ services/edit-trace.ts
│        ├─ pages/home/
│        ├─ pages/plan/
│        ├─ pages/live/
│        └─ pages/preview/
└─ packages/
   └─ contracts/
      ├─ package.json
      ├─ tsconfig.json
      ├─ src/index.ts
      ├─ src/task.ts
      └─ test/task.test.ts
```

## Task 1: Monorepo 与测试基线

**Files:**

- Create: `04-源文件与代码/package.json`
- Create: `04-源文件与代码/tsconfig.json`
- Create: `04-源文件与代码/tsconfig.base.json`
- Create: `04-源文件与代码/vitest.workspace.ts`
- Create: `04-源文件与代码/.gitignore`
- Create: `04-源文件与代码/apps/api/package.json`
- Create: `04-源文件与代码/apps/api/tsconfig.json`
- Create: `04-源文件与代码/apps/miniprogram/package.json`
- Create: `04-源文件与代码/packages/contracts/package.json`
- Create: `04-源文件与代码/packages/contracts/tsconfig.json`

**Interfaces:**

- Consumes: Node.js `v24.18.0 LTS`、npm `v11.16.0`。
- Produces: `npm.cmd test`、`npm.cmd run typecheck` 和 workspace 包名 `@photo-ai/contracts`。

- [ ] **Step 1: 创建根 workspace 配置**

```json
{
  "name": "photo-ai-miniprogram",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev:api": "npm run dev -w @photo-ai/api"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
```

- [ ] **Step 2: 创建 TypeScript 与 Vitest 配置**

`tsconfig.base.json`：

```json
{
  "compilerOptions": {
    "target": "ES2023",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "declaration": true,
    "sourceMap": true,
    "skipLibCheck": true
  }
}
```

`tsconfig.json`：

```json
{
  "extends": "./tsconfig.base.json",
  "files": []
}
```

`vitest.workspace.ts`：

```ts
import { defineWorkspace } from "vitest/config";

export default defineWorkspace([
  "packages/*/vitest.config.ts",
  "apps/api/vitest.config.ts",
  "apps/miniprogram/vitest.config.ts"
]);
```

`packages/contracts/tsconfig.json`：

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "noEmit": true
  },
  "include": ["src/**/*.ts", "test/**/*.ts", "vitest.config.ts"]
}
```

`apps/api/tsconfig.json`：

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "test/**/*.ts", "vitest.config.ts"]
}
```

`apps/miniprogram/tsconfig.json`：

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "noEmit": true,
    "types": ["miniprogram-api-typings"]
  },
  "include": ["miniprogram/**/*.ts"]
}
```

- [ ] **Step 3: 创建 workspace 包清单**

`apps/api/package.json`：

```json
{
  "name": "@photo-ai/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@photo-ai/contracts": "*",
    "fastify": "^5.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0"
  }
}
```

`packages/contracts/package.json`：

```json
{
  "name": "@photo-ai/contracts",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.0.0"
  }
}
```

`.gitignore`：

```gitignore
node_modules/
dist/
coverage/
.env
.env.*
!.env.example
miniprogram_npm/
.DS_Store
*.log
```

`apps/miniprogram/package.json`：

```json
{
  "name": "@photo-ai/miniprogram",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@photo-ai/contracts": "*"
  },
  "devDependencies": {
    "miniprogram-api-typings": "^4.0.0"
  }
}
```

- [ ] **Step 4: 安装依赖**

Run:

```powershell
cd "D:\Documents\workspace\projects\Project-002-修图AI小程序\04-源文件与代码"
npm.cmd install
```

Expected: exit code `0`，生成 `package-lock.json`，三个 workspace 被识别。

- [ ] **Step 5: 验证空基线**

Run:

```powershell
npm.cmd run typecheck
npm.cmd test
```

Expected: TypeScript 项目可加载；Vitest 退出时没有配置解析错误。

- [ ] **Step 6: 提交**

```powershell
git add "04-源文件与代码"
git commit -m "chore: scaffold photo ai monorepo"
```

## Task 2: 共享任务与 EditTrace 契约

**Files:**

- Create: `04-源文件与代码/packages/contracts/src/task.ts`
- Create: `04-源文件与代码/packages/contracts/src/index.ts`
- Create: `04-源文件与代码/packages/contracts/test/task.test.ts`
- Create: `04-源文件与代码/packages/contracts/vitest.config.ts`

**Interfaces:**

- Consumes: Zod。
- Produces: `ToolTypeSchema`、`TaskStatusSchema`、`EditTraceEventSchema`、`CreateTaskInputSchema`、`TaskSnapshotSchema`。

- [ ] **Step 1: 先写契约测试**

```ts
import { describe, expect, it } from "vitest";
import {
  CreateTaskInputSchema,
  EditTraceEventSchema,
  TaskSnapshotSchema
} from "../src/index.js";

describe("task contracts", () => {
  it("accepts a faithful portrait task", () => {
    expect(CreateTaskInputSchema.parse({
      tool: "PORTRAIT_RETOUCH",
      inputAssetId: "demo-portrait-001",
      direction: "NATURAL",
      parameters: { brightness: 0, warmth: 0, naturalness: 80 }
    }).tool).toBe("PORTRAIT_RETOUCH");
  });

  it("rejects an event without sequence", () => {
    expect(() => EditTraceEventSchema.parse({
      eventId: "evt-1",
      taskId: "task-1",
      type: "PLAN_READY"
    })).toThrow();
  });

  it("accepts an awaiting confirmation snapshot", () => {
    expect(TaskSnapshotSchema.parse({
      taskId: "task-1",
      status: "AWAITING_CONFIRMATION",
      tool: "PORTRAIT_RETOUCH",
      lastSequence: 2
    }).status).toBe("AWAITING_CONFIRMATION");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
npm.cmd test -w @photo-ai/contracts
```

Expected: FAIL，原因是 `../src/index.js` 不存在。

- [ ] **Step 3: 实现完整契约**

```ts
import { z } from "zod";

export const ToolTypeSchema = z.enum([
  "PORTRAIT_RETOUCH",
  "QUALITY_ENHANCE",
  "OBJECT_REMOVAL"
]);

export const TaskStatusSchema = z.enum([
  "REVIEWING",
  "DIAGNOSING",
  "AWAITING_CONFIRMATION",
  "QUEUED",
  "PROCESSING",
  "QUALITY_CHECKING",
  "SUCCEEDED",
  "FAILED",
  "REJECTED",
  "CANCELED"
]);

export const EditTraceEventTypeSchema = z.enum([
  "DIAGNOSIS_STARTED",
  "DIAGNOSIS_FINDING",
  "PLAN_READY",
  "STAGE_STARTED",
  "STAGE_COMPLETED",
  "PARAM_DIRECTION_APPLIED",
  "QUALITY_CHECK_STARTED",
  "QUALITY_CHECK_PASSED",
  "QUALITY_CHECK_FAILED",
  "RETRY_STARTED",
  "PREVIEW_READY",
  "TASK_FAILED"
]);

export const EditTraceVisibilitySchema = z.enum(["PREVIEW", "UNLOCKED"]);

export const EditTraceEventSchema = z.object({
  eventId: z.string().min(1),
  taskId: z.string().min(1),
  sequence: z.number().int().positive(),
  type: EditTraceEventTypeSchema,
  phase: z.string().min(1),
  occurredAt: z.string().datetime(),
  visibility: EditTraceVisibilitySchema,
  copyKey: z.string().min(1),
  payload: z.record(z.string(), z.union([
    z.string(),
    z.number(),
    z.boolean()
  ])).default({})
});

export const CreateTaskInputSchema = z.object({
  tool: ToolTypeSchema,
  inputAssetId: z.string().min(1),
  direction: z.enum(["NATURAL", "BRIGHT", "WARM"]),
  parameters: z.object({
    brightness: z.number().min(-100).max(100),
    warmth: z.number().min(-100).max(100),
    naturalness: z.number().min(0).max(100)
  })
});

export const TaskSnapshotSchema = z.object({
  taskId: z.string().min(1),
  status: TaskStatusSchema,
  tool: ToolTypeSchema,
  lastSequence: z.number().int().nonnegative(),
  previewUrl: z.string().url().optional(),
  failureCode: z.string().optional()
});

export type ToolType = z.infer<typeof ToolTypeSchema>;
export type TaskStatus = z.infer<typeof TaskStatusSchema>;
export type EditTraceEvent = z.infer<typeof EditTraceEventSchema>;
export type CreateTaskInput = z.infer<typeof CreateTaskInputSchema>;
export type TaskSnapshot = z.infer<typeof TaskSnapshotSchema>;
```

`src/index.ts`：

```ts
export * from "./task.js";
```

`packages/contracts/vitest.config.ts`：

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    environment: "node",
    include: ["test/**/*.test.ts"]
  }
});
```

- [ ] **Step 4: 运行测试和类型检查**

Run:

```powershell
npm.cmd test -w @photo-ai/contracts
npm.cmd run typecheck -w @photo-ai/contracts
```

Expected: 3 tests PASS，类型检查 exit code `0`。

- [ ] **Step 5: 提交**

```powershell
git add "04-源文件与代码/packages/contracts"
git commit -m "feat: define task and edit trace contracts"
```

## Task 3: 任务状态机

**Files:**

- Create: `04-源文件与代码/apps/api/src/domain/task-machine.ts`
- Create: `04-源文件与代码/apps/api/test/task-machine.test.ts`
- Create: `04-源文件与代码/apps/api/vitest.config.ts`

**Interfaces:**

- Consumes: `TaskStatus`。
- Produces: `canTransition(from, to): boolean`、`transition(from, to): TaskStatus`。

`apps/api/vitest.config.ts` 使用以下完整内容：

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    environment: "node",
    include: ["test/**/*.test.ts"]
  }
});
```

- [ ] **Step 1: 写失败测试**

```ts
import { describe, expect, it } from "vitest";
import { canTransition, transition } from "../src/domain/task-machine.js";

describe("task state machine", () => {
  it("allows the preview happy path", () => {
    expect(canTransition("REVIEWING", "DIAGNOSING")).toBe(true);
    expect(canTransition("DIAGNOSING", "AWAITING_CONFIRMATION")).toBe(true);
    expect(canTransition("AWAITING_CONFIRMATION", "QUEUED")).toBe(true);
    expect(canTransition("QUEUED", "PROCESSING")).toBe(true);
    expect(canTransition("PROCESSING", "QUALITY_CHECKING")).toBe(true);
    expect(canTransition("QUALITY_CHECKING", "SUCCEEDED")).toBe(true);
  });

  it("rejects a terminal transition", () => {
    expect(() => transition("SUCCEEDED", "PROCESSING"))
      .toThrow("Illegal task transition: SUCCEEDED -> PROCESSING");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
npm.cmd test -w @photo-ai/api -- task-machine.test.ts
```

Expected: FAIL，模块不存在。

- [ ] **Step 3: 实现状态机**

```ts
import type { TaskStatus } from "@photo-ai/contracts";

const transitions: Record<TaskStatus, readonly TaskStatus[]> = {
  REVIEWING: ["DIAGNOSING", "REJECTED", "FAILED", "CANCELED"],
  DIAGNOSING: ["AWAITING_CONFIRMATION", "FAILED"],
  AWAITING_CONFIRMATION: ["QUEUED", "CANCELED"],
  QUEUED: ["PROCESSING", "FAILED", "CANCELED"],
  PROCESSING: ["QUALITY_CHECKING", "FAILED"],
  QUALITY_CHECKING: ["PROCESSING", "SUCCEEDED", "FAILED"],
  SUCCEEDED: [],
  FAILED: [],
  REJECTED: [],
  CANCELED: []
};

export function canTransition(from: TaskStatus, to: TaskStatus): boolean {
  return transitions[from].includes(to);
}

export function transition(from: TaskStatus, to: TaskStatus): TaskStatus {
  if (!canTransition(from, to)) {
    throw new Error(`Illegal task transition: ${from} -> ${to}`);
  }
  return to;
}
```

- [ ] **Step 4: 运行测试**

Run:

```powershell
npm.cmd test -w @photo-ai/api -- task-machine.test.ts
```

Expected: 2 tests PASS。

- [ ] **Step 5: 提交**

```powershell
git add "04-源文件与代码/apps/api/src/domain/task-machine.ts" "04-源文件与代码/apps/api/test/task-machine.test.ts"
git commit -m "feat: enforce ai task transitions"
```

## Task 4: AI 精修实况真实性策略

**Files:**

- Create: `04-源文件与代码/apps/api/src/domain/edit-trace-policy.ts`
- Create: `04-源文件与代码/apps/api/test/edit-trace-policy.test.ts`

**Interfaces:**

- Consumes: `EditTraceEvent`。
- Produces: `sanitizeEditTraceEvent(event): EditTraceEvent`。

- [ ] **Step 1: 写敏感字段失败测试**

```ts
import { describe, expect, it } from "vitest";
import { sanitizeEditTraceEvent } from "../src/domain/edit-trace-policy.js";

describe("edit trace policy", () => {
  it("rejects hidden reasoning and provider secrets", () => {
    const base = {
      eventId: "evt-1",
      taskId: "task-1",
      sequence: 1,
      type: "PLAN_READY" as const,
      phase: "PLAN",
      occurredAt: "2026-07-24T00:00:00.000Z",
      visibility: "PREVIEW" as const,
      copyKey: "portrait.plan.ready"
    };

    expect(() => sanitizeEditTraceEvent({
      ...base,
      payload: { chainOfThought: "secret" }
    })).toThrow("Forbidden EditTrace payload key: chainOfThought");

    expect(() => sanitizeEditTraceEvent({
      ...base,
      payload: { providerApiKey: "secret" }
    })).toThrow("Forbidden EditTrace payload key: providerApiKey");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
npm.cmd test -w @photo-ai/api -- edit-trace-policy.test.ts
```

Expected: FAIL，模块不存在。

- [ ] **Step 3: 实现白名单策略**

```ts
import {
  EditTraceEventSchema,
  type EditTraceEvent
} from "@photo-ai/contracts";

const forbiddenKeys = new Set([
  "chainOfThought",
  "hiddenReasoning",
  "providerApiKey",
  "providerSecret",
  "providerRawResponse",
  "internalRoute"
]);

export function sanitizeEditTraceEvent(
  event: EditTraceEvent
): EditTraceEvent {
  const parsed = EditTraceEventSchema.parse(event);
  for (const key of Object.keys(parsed.payload)) {
    if (forbiddenKeys.has(key)) {
      throw new Error(`Forbidden EditTrace payload key: ${key}`);
    }
  }
  return parsed;
}
```

- [ ] **Step 4: 运行测试**

Run:

```powershell
npm.cmd test -w @photo-ai/api -- edit-trace-policy.test.ts
```

Expected: PASS。

- [ ] **Step 5: 提交**

```powershell
git add "04-源文件与代码/apps/api/src/domain/edit-trace-policy.ts" "04-源文件与代码/apps/api/test/edit-trace-policy.test.ts"
git commit -m "feat: protect edit trace authenticity"
```

## Task 5: 仓储、模拟供应商与任务服务

**Files:**

- Create: `04-源文件与代码/apps/api/src/application/task-service.ts`
- Create: `04-源文件与代码/apps/api/src/infrastructure/in-memory-task-repository.ts`
- Create: `04-源文件与代码/apps/api/src/infrastructure/mock-image-provider.ts`
- Create: `04-源文件与代码/apps/api/test/task-service.test.ts`

**Interfaces:**

- Consumes: `CreateTaskInput`、状态机、真实性策略。
- Produces: `TaskRepository`、`ImageProvider`、`TaskService.create()`、`TaskService.confirmAndRunPreview()`、`TaskService.getEvents()`。

- [ ] **Step 1: 写纵向服务测试**

```ts
import { describe, expect, it } from "vitest";
import { TaskService } from "../src/application/task-service.js";
import { InMemoryTaskRepository } from "../src/infrastructure/in-memory-task-repository.js";
import { MockImageProvider } from "../src/infrastructure/mock-image-provider.js";

describe("TaskService", () => {
  it("emits truthful events and produces a preview", async () => {
    const service = new TaskService(
      new InMemoryTaskRepository(),
      new MockImageProvider()
    );

    const created = await service.create({
      tool: "PORTRAIT_RETOUCH",
      inputAssetId: "demo-portrait-001",
      direction: "NATURAL",
      parameters: { brightness: 0, warmth: 0, naturalness: 80 }
    });

    expect(created.status).toBe("AWAITING_CONFIRMATION");

    const finished = await service.confirmAndRunPreview(created.taskId);
    expect(finished.status).toBe("SUCCEEDED");
    expect(finished.previewUrl).toContain("demo-preview");

    const events = await service.getEvents(created.taskId, 0);
    expect(events.map((event) => event.type)).toEqual([
      "DIAGNOSIS_STARTED",
      "DIAGNOSIS_FINDING",
      "PLAN_READY",
      "STAGE_STARTED",
      "PARAM_DIRECTION_APPLIED",
      "STAGE_COMPLETED",
      "QUALITY_CHECK_STARTED",
      "QUALITY_CHECK_PASSED",
      "PREVIEW_READY"
    ]);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
npm.cmd test -w @photo-ai/api -- task-service.test.ts
```

Expected: FAIL，服务和适配器不存在。

- [ ] **Step 3: 定义服务接口**

`task-service.ts` 必须定义：

```ts
import type {
  CreateTaskInput,
  EditTraceEvent,
  TaskSnapshot
} from "@photo-ai/contracts";

export interface StoredTask extends TaskSnapshot {
  input: CreateTaskInput;
}

export interface TaskRepository {
  save(task: StoredTask): Promise<void>;
  find(taskId: string): Promise<StoredTask | undefined>;
  appendEvent(event: EditTraceEvent): Promise<void>;
  eventsAfter(taskId: string, sequence: number): Promise<EditTraceEvent[]>;
}

export interface ProviderRunResult {
  previewUrl: string;
  events: Omit<EditTraceEvent, "eventId" | "taskId" | "sequence">[];
}

export interface ImageProvider {
  runPreview(input: CreateTaskInput): Promise<ProviderRunResult>;
}

import { randomUUID } from "node:crypto";
import { transition } from "../domain/task-machine.js";
import { sanitizeEditTraceEvent } from "../domain/edit-trace-policy.js";

type ProviderEvent =
  Omit<EditTraceEvent, "eventId" | "taskId" | "sequence">;

export class TaskService {
  public constructor(
    private readonly repository: TaskRepository,
    private readonly provider: ImageProvider
  ) {}

  public async create(input: CreateTaskInput): Promise<TaskSnapshot> {
    const task: StoredTask = {
      taskId: randomUUID(),
      status: "REVIEWING",
      tool: input.tool,
      lastSequence: 0,
      input
    };
    await this.repository.save(task);

    task.status = transition(task.status, "DIAGNOSING");
    await this.repository.save(task);
    await this.append(task, {
      type: "DIAGNOSIS_STARTED",
      phase: "DIAGNOSIS",
      occurredAt: new Date().toISOString(),
      visibility: "PREVIEW",
      copyKey: "portrait.diagnosis.started",
      payload: {}
    });
    await this.append(task, {
      type: "DIAGNOSIS_FINDING",
      phase: "DIAGNOSIS",
      occurredAt: new Date().toISOString(),
      visibility: "PREVIEW",
      copyKey: "portrait.diagnosis.light",
      payload: { finding: "FACE_SHADOW_AND_BACKGROUND_HIGHLIGHT" }
    });
    await this.append(task, {
      type: "PLAN_READY",
      phase: "PLAN",
      occurredAt: new Date().toISOString(),
      visibility: "PREVIEW",
      copyKey: "portrait.plan.natural",
      payload: { direction: input.direction }
    });
    task.status = transition(task.status, "AWAITING_CONFIRMATION");
    await this.repository.save(task);
    return this.snapshot(task);
  }

  public async confirmAndRunPreview(
    taskId: string
  ): Promise<TaskSnapshot> {
    const task = await this.requireTask(taskId);
    task.status = transition(task.status, "QUEUED");
    await this.repository.save(task);
    task.status = transition(task.status, "PROCESSING");
    await this.repository.save(task);

    const result = await this.provider.runPreview(task.input);
    for (const event of result.events) {
      if (event.type === "QUALITY_CHECK_STARTED") {
        task.status = transition(task.status, "QUALITY_CHECKING");
        await this.repository.save(task);
      }
      await this.append(task, event);
    }
    task.status = transition(task.status, "SUCCEEDED");
    task.previewUrl = result.previewUrl;
    await this.repository.save(task);
    return this.snapshot(task);
  }

  public async get(taskId: string): Promise<TaskSnapshot> {
    return this.snapshot(await this.requireTask(taskId));
  }

  public async getEvents(
    taskId: string,
    afterSequence: number
  ): Promise<EditTraceEvent[]> {
    await this.requireTask(taskId);
    return this.repository.eventsAfter(taskId, afterSequence);
  }

  private async requireTask(taskId: string): Promise<StoredTask> {
    const task = await this.repository.find(taskId);
    if (!task) {
      throw new Error("TASK_NOT_FOUND");
    }
    return task;
  }

  private async append(
    task: StoredTask,
    event: ProviderEvent
  ): Promise<void> {
    const fullEvent = sanitizeEditTraceEvent({
      ...event,
      eventId: randomUUID(),
      taskId: task.taskId,
      sequence: task.lastSequence + 1
    });
    task.lastSequence = fullEvent.sequence;
    await this.repository.appendEvent(fullEvent);
    await this.repository.save(task);
  }

  private snapshot(task: StoredTask): TaskSnapshot {
    return {
      taskId: task.taskId,
      status: task.status,
      tool: task.tool,
      lastSequence: task.lastSequence,
      ...(task.previewUrl ? { previewUrl: task.previewUrl } : {}),
      ...(task.failureCode ? { failureCode: task.failureCode } : {})
    };
  }
}
```

`in-memory-task-repository.ts`：

```ts
import type { EditTraceEvent } from "@photo-ai/contracts";
import type {
  StoredTask,
  TaskRepository
} from "../application/task-service.js";

export class InMemoryTaskRepository implements TaskRepository {
  private readonly tasks = new Map<string, StoredTask>();
  private readonly events = new Map<string, EditTraceEvent[]>();

  public async save(task: StoredTask): Promise<void> {
    this.tasks.set(task.taskId, structuredClone(task));
  }

  public async find(taskId: string): Promise<StoredTask | undefined> {
    const task = this.tasks.get(taskId);
    return task ? structuredClone(task) : undefined;
  }

  public async appendEvent(event: EditTraceEvent): Promise<void> {
    const current = this.events.get(event.taskId) ?? [];
    current.push(structuredClone(event));
    this.events.set(event.taskId, current);
  }

  public async eventsAfter(
    taskId: string,
    sequence: number
  ): Promise<EditTraceEvent[]> {
    return (this.events.get(taskId) ?? [])
      .filter((event) => event.sequence > sequence)
      .map((event) => structuredClone(event));
  }
}
```

- [ ] **Step 4: 实现确定性模拟供应商**

`mock-image-provider.ts`：

```ts
import type {
  CreateTaskInput,
  EditTraceEvent
} from "@photo-ai/contracts";
import type {
  ImageProvider,
  ProviderRunResult
} from "../application/task-service.js";

type ProviderEvent =
  Omit<EditTraceEvent, "eventId" | "taskId" | "sequence">;

function atNow(
  event: Omit<ProviderEvent, "occurredAt">
): ProviderEvent {
  return { ...event, occurredAt: new Date().toISOString() };
}

export class MockImageProvider implements ImageProvider {
  public async runPreview(
    input: CreateTaskInput
  ): Promise<ProviderRunResult> {
    if (input.tool !== "PORTRAIT_RETOUCH") {
      throw new Error("MOCK_PROVIDER_UNSUPPORTED_TOOL");
    }
    return {
      previewUrl:
        "https://example.invalid/demo-preview/portrait-natural.jpg",
      events: [
        atNow({
          type: "STAGE_STARTED",
          phase: "RETOUCH",
          visibility: "PREVIEW",
          copyKey: "portrait.stage.retouch.started",
          payload: { stage: "LOCAL_LIGHT_AND_SKIN" }
        }),
        atNow({
          type: "PARAM_DIRECTION_APPLIED",
          phase: "RETOUCH",
          visibility: "PREVIEW",
          copyKey: "portrait.parameter.direction",
          payload: { direction: input.direction, level: "MODERATE" }
        }),
        atNow({
          type: "STAGE_COMPLETED",
          phase: "RETOUCH",
          visibility: "PREVIEW",
          copyKey: "portrait.stage.retouch.completed",
          payload: { stage: "LOCAL_LIGHT_AND_SKIN" }
        }),
        atNow({
          type: "QUALITY_CHECK_STARTED",
          phase: "QUALITY",
          visibility: "PREVIEW",
          copyKey: "quality.started",
          payload: {}
        }),
        atNow({
          type: "QUALITY_CHECK_PASSED",
          phase: "QUALITY",
          visibility: "PREVIEW",
          copyKey: "quality.identity.passed",
          payload: { check: "IDENTITY_CONSISTENCY" }
        }),
        atNow({
          type: "PREVIEW_READY",
          phase: "DELIVERY",
          visibility: "PREVIEW",
          copyKey: "preview.ready",
          payload: { watermarked: true, downloadable: false }
        })
      ]
    };
  }
}
```

- [ ] **Step 5: 运行服务测试**

Run:

```powershell
npm.cmd test -w @photo-ai/api -- task-service.test.ts
```

Expected: PASS，事件顺序与测试完全一致。

- [ ] **Step 6: 提交**

```powershell
git add "04-源文件与代码/apps/api/src/application" "04-源文件与代码/apps/api/src/infrastructure" "04-源文件与代码/apps/api/test/task-service.test.ts"
git commit -m "feat: orchestrate mock portrait preview"
```

## Task 6: Fastify 任务 API

**Files:**

- Create: `04-源文件与代码/apps/api/src/routes/tasks.ts`
- Create: `04-源文件与代码/apps/api/src/app.ts`
- Create: `04-源文件与代码/apps/api/src/server.ts`
- Create: `04-源文件与代码/apps/api/test/tasks-api.test.ts`

**Interfaces:**

- Consumes: `TaskService`。
- Produces:
  - `POST /v1/tasks`
  - `POST /v1/tasks/:taskId/preview`
  - `GET /v1/tasks/:taskId`
  - `GET /v1/tasks/:taskId/events?afterSequence=0`

- [ ] **Step 1: 写 API 测试**

```ts
import { describe, expect, it } from "vitest";
import { buildApp } from "../src/app.js";

describe("tasks API", () => {
  it("creates, runs and reads a portrait preview", async () => {
    const app = buildApp();
    const created = await app.inject({
      method: "POST",
      url: "/v1/tasks",
      payload: {
        tool: "PORTRAIT_RETOUCH",
        inputAssetId: "demo-portrait-001",
        direction: "NATURAL",
        parameters: { brightness: 0, warmth: 0, naturalness: 80 }
      }
    });
    expect(created.statusCode).toBe(201);
    const task = created.json();

    const preview = await app.inject({
      method: "POST",
      url: `/v1/tasks/${task.taskId}/preview`
    });
    expect(preview.statusCode).toBe(202);

    const events = await app.inject({
      method: "GET",
      url: `/v1/tasks/${task.taskId}/events?afterSequence=0`
    });
    expect(events.statusCode).toBe(200);
    expect(events.json().items.at(-1).type).toBe("PREVIEW_READY");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
npm.cmd test -w @photo-ai/api -- tasks-api.test.ts
```

Expected: FAIL，`buildApp` 不存在。

- [ ] **Step 3: 实现路由**

`routes/tasks.ts`：

```ts
import type { FastifyInstance, FastifyReply } from "fastify";
import { CreateTaskInputSchema } from "@photo-ai/contracts";
import type { TaskService } from "../application/task-service.js";

function sendDomainError(reply: FastifyReply, error: unknown) {
  const message = error instanceof Error ? error.message : "UNKNOWN_ERROR";
  if (message === "TASK_NOT_FOUND") {
    return reply.code(404).send({ code: "TASK_NOT_FOUND" });
  }
  if (message.startsWith("Illegal task transition:")) {
    return reply.code(409).send({ code: "ILLEGAL_TASK_STATE" });
  }
  return reply.code(500).send({ code: "INTERNAL_ERROR" });
}

export async function registerTaskRoutes(
  app: FastifyInstance,
  service: TaskService
): Promise<void> {
  app.post("/v1/tasks", async (request, reply) => {
    const parsed = CreateTaskInputSchema.safeParse(request.body);
    if (!parsed.success) {
      return reply.code(400).send({
        code: "INVALID_TASK_INPUT",
        issues: parsed.error.issues.map((issue) => ({
          path: issue.path.join("."),
          message: issue.message
        }))
      });
    }
    const task = await service.create(parsed.data);
    return reply.code(201).send(task);
  });

  app.post<{
    Params: { taskId: string };
  }>("/v1/tasks/:taskId/preview", async (request, reply) => {
    try {
      const task = await service.confirmAndRunPreview(
        request.params.taskId
      );
      return reply.code(202).send(task);
    } catch (error) {
      return sendDomainError(reply, error);
    }
  });

  app.get<{
    Params: { taskId: string };
  }>("/v1/tasks/:taskId", async (request, reply) => {
    try {
      return reply.send(await service.get(request.params.taskId));
    } catch (error) {
      return sendDomainError(reply, error);
    }
  });

  app.get<{
    Params: { taskId: string };
    Querystring: { afterSequence?: string };
  }>("/v1/tasks/:taskId/events", async (request, reply) => {
    const raw = request.query.afterSequence ?? "0";
    const afterSequence = Number(raw);
    if (!Number.isInteger(afterSequence) || afterSequence < 0) {
      return reply.code(400).send({ code: "INVALID_SEQUENCE" });
    }
    try {
      const items = await service.getEvents(
        request.params.taskId,
        afterSequence
      );
      return reply.send({
        items,
        nextSequence:
          items.length > 0
            ? items[items.length - 1]!.sequence
            : afterSequence
      });
    } catch (error) {
      return sendDomainError(reply, error);
    }
  });
}
```

- [ ] **Step 4: 实现应用装配**

`app.ts`：

```ts
import Fastify from "fastify";
import { TaskService } from "./application/task-service.js";
import { InMemoryTaskRepository } from
  "./infrastructure/in-memory-task-repository.js";
import { MockImageProvider } from
  "./infrastructure/mock-image-provider.js";
import { registerTaskRoutes } from "./routes/tasks.js";

export function buildApp() {
  const app = Fastify({ logger: false });
  const service = new TaskService(
    new InMemoryTaskRepository(),
    new MockImageProvider()
  );
  void registerTaskRoutes(app, service);
  return app;
}
```

`server.ts`：

```ts
import { buildApp } from "./app.js";

const app = buildApp();

try {
  await app.listen({ host: "127.0.0.1", port: 3100 });
} catch (error) {
  app.log.error(error);
  process.exitCode = 1;
}
```

- [ ] **Step 5: 运行 API 测试**

Run:

```powershell
npm.cmd test -w @photo-ai/api -- tasks-api.test.ts
npm.cmd run typecheck -w @photo-ai/api
```

Expected: API 测试 PASS，类型检查 exit code `0`。

- [ ] **Step 6: 提交**

```powershell
git add "04-源文件与代码/apps/api/src" "04-源文件与代码/apps/api/test/tasks-api.test.ts"
git commit -m "feat: expose portrait preview api"
```

## Task 7: 小程序 API 与 EditTrace 增量合并

**Files:**

- Create: `04-源文件与代码/apps/miniprogram/tsconfig.json`
- Create: `04-源文件与代码/apps/miniprogram/project.config.json`
- Create: `04-源文件与代码/apps/miniprogram/miniprogram/services/api.ts`
- Create: `04-源文件与代码/apps/miniprogram/miniprogram/services/edit-trace.ts`
- Create: `04-源文件与代码/apps/miniprogram/vitest.config.ts`
- Create: `04-源文件与代码/apps/miniprogram/test/edit-trace.test.ts`

**Interfaces:**

- Consumes: API JSON 契约。
- Produces: `createTask()`、`runPreview()`、`getTask()`、`getEvents()`、`mergeEvents()`。

- [ ] **Step 1: 创建请求封装**

`project.config.json`：

```json
{
  "description": "Project-002 修图 AI 小程序",
  "compileType": "miniprogram",
  "miniprogramRoot": "miniprogram/",
  "setting": {
    "es6": true,
    "enhance": true,
    "minified": false,
    "urlCheck": false
  },
  "appid": "touristappid",
  "projectname": "project-002-photo-ai"
}
```

`services/api.ts`：

```ts
import type {
  CreateTaskInput,
  EditTraceEvent,
  TaskSnapshot
} from "@photo-ai/contracts";

const API_BASE = "http://127.0.0.1:3100";

function request<T>(
  options: WechatMiniprogram.RequestOption
): Promise<T> {
  return new Promise((resolve, reject) => {
    wx.request({
      ...options,
      url: `${API_BASE}${options.url}`,
      success(response) {
        if (response.statusCode >= 200 && response.statusCode < 300) {
          resolve(response.data as T);
          return;
        }
        reject(new Error(`API_${response.statusCode}`));
      },
      fail: reject
    });
  });
}
```

导出四个函数：

```ts
export const createTask = (input: CreateTaskInput) =>
  request<TaskSnapshot>({
    method: "POST",
    url: "/v1/tasks",
    data: input
  });

export const runPreview = (taskId: string) =>
  request<TaskSnapshot>({
    method: "POST",
    url: `/v1/tasks/${taskId}/preview`
  });

export const getTask = (taskId: string) =>
  request<TaskSnapshot>({
    method: "GET",
    url: `/v1/tasks/${taskId}`
  });

export const getEvents = (taskId: string, afterSequence: number) =>
  request<{ items: EditTraceEvent[]; nextSequence: number }>({
    method: "GET",
    url: `/v1/tasks/${taskId}/events?afterSequence=${afterSequence}`
  });
```

- [ ] **Step 2: 实现事件去重与排序**

`services/edit-trace.ts`：

```ts
import type { EditTraceEvent } from "@photo-ai/contracts";

export function mergeEvents(
  current: EditTraceEvent[],
  incoming: EditTraceEvent[]
): EditTraceEvent[] {
  const byId = new Map(current.map((event) => [event.eventId, event]));
  for (const event of incoming) {
    byId.set(event.eventId, event);
  }
  return [...byId.values()].sort((a, b) => a.sequence - b.sequence);
}
```

- [ ] **Step 3: 写并运行事件合并测试**

`vitest.config.ts`：

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    environment: "node",
    include: ["test/**/*.test.ts"]
  }
});
```

`test/edit-trace.test.ts`：

```ts
import { describe, expect, it } from "vitest";
import type { EditTraceEvent } from "@photo-ai/contracts";
import { mergeEvents } from "../miniprogram/services/edit-trace";

function event(eventId: string, sequence: number): EditTraceEvent {
  return {
    eventId,
    taskId: "task-1",
    sequence,
    type: "STAGE_STARTED",
    phase: "RETOUCH",
    occurredAt: "2026-07-24T00:00:00.000Z",
    visibility: "PREVIEW",
    copyKey: "portrait.stage.retouch.started",
    payload: {}
  };
}

describe("mergeEvents", () => {
  it("deduplicates by event id and sorts by sequence", () => {
    expect(mergeEvents(
      [event("evt-2", 2)],
      [event("evt-1", 1), event("evt-2", 2)]
    ).map((item) => item.eventId)).toEqual(["evt-1", "evt-2"]);
  });
});
```

Run:

```powershell
npm.cmd test -w @photo-ai/miniprogram
```

Expected: 1 test PASS。

- [ ] **Step 4: 类型检查**

Run:

```powershell
npm.cmd run typecheck -w @photo-ai/miniprogram
```

Expected: exit code `0`。

- [ ] **Step 5: 提交**

```powershell
git add "04-源文件与代码/apps/miniprogram"
git commit -m "feat: add miniprogram task client"
```

## Task 8: 原生小程序四页纵向体验

**Files:**

- Create: `04-源文件与代码/apps/miniprogram/miniprogram/app.json`
- Create: `04-源文件与代码/apps/miniprogram/miniprogram/app.ts`
- Create: `04-源文件与代码/apps/miniprogram/miniprogram/app.wxss`
- Create: `04-源文件与代码/apps/miniprogram/miniprogram/sitemap.json`
- Create: `04-源文件与代码/apps/miniprogram/miniprogram/assets/demo-before.svg`
- Create: `04-源文件与代码/apps/miniprogram/miniprogram/assets/demo-after.svg`
- Create: `04-源文件与代码/apps/miniprogram/miniprogram/pages/home/index.{json,ts,wxml,wxss}`
- Create: `04-源文件与代码/apps/miniprogram/miniprogram/pages/plan/index.{json,ts,wxml,wxss}`
- Create: `04-源文件与代码/apps/miniprogram/miniprogram/pages/live/index.{json,ts,wxml,wxss}`
- Create: `04-源文件与代码/apps/miniprogram/miniprogram/pages/preview/index.{json,ts,wxml,wxss}`

**Interfaces:**

- Consumes: Task API。
- Produces: 首页 → 方案确认 → AI 精修实况 → 水印预览的可操作路径。

- [ ] **Step 1: 配置页面**

```json
{
  "pages": [
    "pages/home/index",
    "pages/plan/index",
    "pages/live/index",
    "pages/preview/index"
  ],
  "window": {
    "navigationBarTitleText": "AI 忠实精修",
    "navigationBarBackgroundColor": "#F7F5F0",
    "navigationBarTextStyle": "black",
    "backgroundColor": "#F7F5F0"
  },
  "style": "v2",
  "sitemapLocation": "sitemap.json"
}
```

`app.ts`：

```ts
App({});
```

`sitemap.json`：

```json
{
  "desc": "Project-002 development sitemap",
  "rules": [{ "action": "disallow", "page": "*" }]
}
```

`app.wxss`：

```css
page {
  background: #f7f5f0;
  color: #171719;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}
.page {
  min-height: 100vh;
  padding: 32rpx;
  box-sizing: border-box;
}
.card {
  background: #ffffff;
  border-radius: 24rpx;
  padding: 28rpx;
  box-shadow: 0 12rpx 36rpx rgba(23, 23, 25, 0.07);
}
.button-primary {
  height: 96rpx;
  border-radius: 28rpx;
  background: #171719;
  color: #ffffff;
  font-size: 30rpx;
  font-weight: 700;
  line-height: 96rpx;
}
.button-primary[disabled] {
  opacity: 0.48;
}
.muted {
  color: #77727f;
  font-size: 24rpx;
  line-height: 1.6;
}
.accent {
  color: #6c58c9;
}
```

- [ ] **Step 2: 实现首页**

`pages/home/index.json`：

```json
{ "navigationBarTitleText": "AI 忠实精修" }
```

`pages/home/index.ts`：

```ts
Page({
  openPortrait() {
    wx.navigateTo({ url: "/pages/plan/index" });
  }
});
```

`pages/home/index.wxml`：

```xml
<view class="page home">
  <view class="eyebrow">忠实 AI 精修</view>
  <view class="title">让照片更好，还是你</view>
  <view class="subtitle">保留五官、肤质与原图构图的专业精修</view>

  <view class="hero card">
    <image class="hero-image" src="/assets/demo-before.svg" mode="aspectFill" />
    <view class="hero-copy">
      <view class="hero-name">AI 智能人像精修</view>
      <view class="muted">自然光影 · 真实肤质 · 身份稳定</view>
      <button class="button-primary" bindtap="openPortrait">
        使用示例照片体验
      </button>
    </view>
  </view>

  <view class="tool-grid">
    <view class="tool-card disabled">
      <view class="tool-name">画质增强</view>
      <view class="muted">阶段 B 接入</view>
    </view>
    <view class="tool-card disabled">
      <view class="tool-name">杂物消除</view>
      <view class="muted">阶段 B 接入</view>
    </view>
  </view>
</view>
```

`pages/home/index.wxss`：

```css
.home { padding-top: 64rpx; }
.eyebrow { color: #6c58c9; font-size: 24rpx; font-weight: 700; }
.title { margin-top: 12rpx; font-size: 56rpx; font-weight: 800; letter-spacing: -2rpx; }
.subtitle { margin-top: 16rpx; color: #716d76; font-size: 28rpx; line-height: 1.6; }
.hero { margin-top: 40rpx; padding: 0; overflow: hidden; }
.hero-image { display: block; width: 100%; height: 520rpx; background: #ddd3cc; }
.hero-copy { padding: 28rpx; }
.hero-name { font-size: 34rpx; font-weight: 750; margin-bottom: 8rpx; }
.hero .button-primary { margin-top: 28rpx; }
.tool-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20rpx; margin-top: 24rpx; }
.tool-card { border-radius: 24rpx; padding: 28rpx; background: #ebe7ff; }
.tool-card:nth-child(2) { background: #e8f2ef; }
.tool-card.disabled { opacity: 0.65; }
.tool-name { font-size: 28rpx; font-weight: 700; margin-bottom: 6rpx; }
```

`assets/demo-before.svg`：

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="720" height="900" viewBox="0 0 720 900">
  <defs><linearGradient id="b" x1="0" y1="0" x2="1" y2="1"><stop stop-color="#d8c8bc"/><stop offset="1" stop-color="#756d75"/></linearGradient></defs>
  <rect width="720" height="900" fill="url(#b)"/>
  <circle cx="360" cy="330" r="150" fill="#d1a98f"/>
  <path d="M190 900c20-220 95-330 170-330s150 110 170 330" fill="#27252c"/>
  <path d="M240 305c20-145 220-180 260-5-80-35-170-45-260 5z" fill="#2f2a2a"/>
  <rect width="720" height="900" fill="#1d1a22" opacity=".14"/>
  <text x="32" y="860" fill="#fff" font-size="28" font-family="sans-serif">示例原图 · 光线偏暗</text>
</svg>
```

首页只展示人像精修可操作卡片；画质增强和杂物消除显示“阶段 B 接入”，不提供创建按钮。

- [ ] **Step 3: 实现方案确认页**

`pages/plan/index.json`：

```json
{ "navigationBarTitleText": "确认精修方案" }
```

`pages/plan/index.ts`：

```ts
import { createTask } from "../../services/api";

const input = {
  tool: "PORTRAIT_RETOUCH" as const,
  inputAssetId: "demo-portrait-001",
  direction: "NATURAL" as const,
  parameters: {
    brightness: 0,
    warmth: 0,
    naturalness: 80
  }
};

Page({
  data: {
    submitting: false,
    error: ""
  },
  onShow() {
    this.setData({ submitting: false });
  },
  async startPreview() {
    if (this.data.submitting) return;
    this.setData({ submitting: true, error: "" });
    try {
      const task = await createTask(input);
      wx.navigateTo({
        url: `/pages/live/index?taskId=${task.taskId}`
      });
    } catch {
      this.setData({
        submitting: false,
        error: "暂时无法创建任务，请确认本地 API 已启动后重试。"
      });
    }
  }
});
```

`pages/plan/index.wxml`：

```xml
<view class="page">
  <view class="plan-photo card">
    <image src="/assets/demo-before.svg" mode="aspectFill" />
  </view>
  <view class="card plan">
    <view class="badge">AI 推荐</view>
    <view class="plan-title">自然原貌</view>
    <view class="muted">照片存在面部暗部与背景高光，建议适度恢复层次并保留真实肤质。</view>
    <view class="promise">
      <view>✓ 保留五官、脸型与人物身份</view>
      <view>✓ 不改变姿势、服装和主要构图</view>
      <view>✓ 自然程度 80%</view>
    </view>
    <button class="button-primary" loading="{{submitting}}" disabled="{{submitting}}" bindtap="startPreview">
      开始生成水印预览
    </button>
    <view wx:if="{{error}}" class="error">{{error}}</view>
  </view>
</view>
```

`pages/plan/index.wxss`：

```css
.plan-photo { padding: 0; overflow: hidden; }
.plan-photo image { display: block; width: 100%; height: 480rpx; }
.plan { margin-top: 24rpx; }
.badge { display: inline-block; padding: 10rpx 18rpx; border-radius: 999rpx; color: #5744b2; background: #ebe7ff; font-size: 22rpx; font-weight: 700; }
.plan-title { margin: 22rpx 0 10rpx; font-size: 40rpx; font-weight: 800; }
.promise { margin: 28rpx 0; display: grid; gap: 14rpx; font-size: 26rpx; line-height: 1.5; }
.error { margin-top: 20rpx; color: #a4464c; font-size: 24rpx; }
```

- [ ] **Step 4: 实现 AI 精修实况页**

`pages/live/index.json`：

```json
{ "navigationBarTitleText": "AI 精修实况" }
```

`pages/live/index.ts`：

```ts
import type { EditTraceEvent } from "@photo-ai/contracts";
import { getEvents, runPreview } from "../../services/api";
import { mergeEvents } from "../../services/edit-trace";

type RenderEvent = EditTraceEvent & { text: string };

const copy: Record<string, string> = {
  "portrait.diagnosis.started": "正在分析照片的光线、肤质与主体结构",
  "portrait.diagnosis.light": "检测到面部暗部与背景高光差异",
  "portrait.plan.natural": "已制定保留真实肤质的自然精修方案",
  "portrait.stage.retouch.started": "正在恢复人物局部光影与肤质层次",
  "portrait.parameter.direction": "已应用适中的自然精修方向",
  "portrait.stage.retouch.completed": "人物局部调整已完成",
  "quality.started": "正在检查身份与非目标区域稳定性",
  "quality.identity.passed": "人物身份一致性检查通过",
  "preview.ready": "水印预览已生成"
};

let pending: RenderEvent[] = [];
let revealTimer: ReturnType<typeof setTimeout> | undefined;
let pollTimer: ReturnType<typeof setTimeout> | undefined;
let disposed = false;

Page({
  data: {
    taskId: "",
    allEvents: [] as EditTraceEvent[],
    visibleEvents: [] as RenderEvent[],
    lastSequence: 0,
    ready: false,
    error: ""
  },
  onLoad(options: Record<string, string>) {
    const taskId = options.taskId;
    if (!taskId) {
      this.setData({ error: "任务编号缺失，请返回重新创建任务。" });
      return;
    }
    disposed = false;
    pending = [];
    this.setData({ taskId });
    void this.start();
  },
  onUnload() {
    disposed = true;
    if (revealTimer) clearTimeout(revealTimer);
    if (pollTimer) clearTimeout(pollTimer);
  },
  async start() {
    try {
      await runPreview(this.data.taskId);
      await this.poll();
    } catch {
      this.setData({ error: "精修任务暂时无法继续，请稍后重试。" });
    }
  },
  async poll() {
    if (disposed || this.data.ready) return;
    try {
      const response = await getEvents(
        this.data.taskId,
        this.data.lastSequence
      );
      const allEvents = mergeEvents(
        this.data.allEvents,
        response.items
      );
      const existingIds = new Set(
        this.data.visibleEvents.map((event) => event.eventId)
      );
      const queuedIds = new Set(pending.map((event) => event.eventId));
      pending.push(
        ...response.items
          .filter((event) =>
            !existingIds.has(event.eventId) &&
            !queuedIds.has(event.eventId)
          )
          .map((event) => ({
            ...event,
            text: copy[event.copyKey] ?? `任务事件：${event.copyKey}`
          }))
      );
      this.setData({
        allEvents,
        lastSequence: response.nextSequence
      });
      this.revealNext();
      if (!response.items.some((event) => event.type === "PREVIEW_READY")) {
        pollTimer = setTimeout(() => void this.poll(), 500);
      }
    } catch {
      this.setData({ error: "无法获取精修进度，请检查网络后重试。" });
    }
  },
  revealNext() {
    if (disposed || revealTimer || pending.length === 0) return;
    const next = pending.shift()!;
    revealTimer = setTimeout(() => {
      revealTimer = undefined;
      const visibleEvents = [...this.data.visibleEvents, next];
      this.setData({
        visibleEvents,
        ready: next.type === "PREVIEW_READY" || this.data.ready
      });
      this.revealNext();
    }, 420);
  },
  openPreview() {
    wx.navigateTo({
      url: `/pages/preview/index?taskId=${this.data.taskId}`
    });
  },
  retry() {
    this.setData({ error: "" });
    void this.poll();
  }
});
```

`pages/live/index.wxml`：

```xml
<view class="page live">
  <view class="live-label">AI 精修实况</view>
  <view class="live-title">正在认真处理这张照片</view>
  <view class="muted">以下内容来自真实任务事件，不展示或伪造模型隐藏推理。</view>

  <view class="stream">
    <view wx:for="{{visibleEvents}}" wx:key="eventId" class="event-card">
      <view class="event-dot">✓</view>
      <view class="event-copy">{{item.text}}</view>
    </view>
  </view>

  <view wx:if="{{!ready && !error}}" class="working">
    <view class="pulse"></view>
    <text>任务会在离开页面后继续</text>
  </view>
  <view wx:if="{{error}}" class="error card">
    <view>{{error}}</view>
    <button bindtap="retry">重新获取状态</button>
  </view>
  <button wx:if="{{ready}}" class="button-primary" bindtap="openPreview">
    查看水印预览
  </button>
</view>
```

`pages/live/index.wxss`：

```css
.live { padding-top: 56rpx; }
.live-label { color: #9f8cff; font-size: 24rpx; font-weight: 700; }
.live-title { margin: 12rpx 0; font-size: 46rpx; font-weight: 800; }
.stream { margin: 40rpx 0; display: grid; gap: 18rpx; }
.event-card { display: flex; gap: 20rpx; padding: 26rpx; border-radius: 22rpx; background: #ffffff; box-shadow: 0 8rpx 28rpx rgba(23,23,25,.06); }
.event-dot { color: #6c58c9; font-weight: 800; }
.event-copy { flex: 1; font-size: 27rpx; line-height: 1.55; }
.working { display: flex; align-items: center; gap: 16rpx; color: #77727f; font-size: 24rpx; }
.pulse { width: 18rpx; height: 18rpx; border-radius: 50%; background: #6c58c9; animation: pulse 1.2s infinite; }
.error { color: #a4464c; }
.error button { margin-top: 18rpx; }
.button-primary { margin-top: 30rpx; }
@keyframes pulse { 50% { transform: scale(.65); opacity: .45; } }
```

定时器只拉取和逐条展示服务端已经生成的真实事件，不产生本地虚构事件。

- [ ] **Step 5: 实现预览页**

`assets/demo-after.svg`：

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="720" height="900" viewBox="0 0 720 900">
  <defs><linearGradient id="a" x1="0" y1="0" x2="1" y2="1"><stop stop-color="#ead9c9"/><stop offset="1" stop-color="#8b7f88"/></linearGradient></defs>
  <rect width="720" height="900" fill="url(#a)"/>
  <circle cx="360" cy="330" r="150" fill="#dfb79d"/>
  <path d="M190 900c20-220 95-330 170-330s150 110 170 330" fill="#302d35"/>
  <path d="M240 305c20-145 220-180 260-5-80-35-170-45-260 5z" fill="#2f2a2a"/>
  <text x="360" y="470" text-anchor="middle" fill="#fff" fill-opacity=".72" font-size="30" font-weight="700" font-family="sans-serif" transform="rotate(-15 360 470)">效果预览 · 不可保存</text>
</svg>
```

`pages/preview/index.json`：

```json
{ "navigationBarTitleText": "水印预览" }
```

`pages/preview/index.ts`：

```ts
Page({
  data: { showAfter: true },
  showBefore() {
    this.setData({ showAfter: false });
  },
  showAfter() {
    this.setData({ showAfter: true });
  },
  adjustAgain() {
    wx.redirectTo({ url: "/pages/plan/index" });
  }
});
```

`pages/preview/index.wxml`：

```xml
<view class="page">
  <view class="preview card">
    <image
      class="preview-image"
      src="{{showAfter ? '/assets/demo-after.svg' : '/assets/demo-before.svg'}}"
      mode="aspectFill"
    />
    <view class="preview-tag">{{showAfter ? '精修后' : '原图'}}</view>
  </view>
  <view class="compare">
    <button class="{{!showAfter ? 'active' : ''}}" bindtap="showBefore">原图</button>
    <button class="{{showAfter ? 'active' : ''}}" bindtap="showAfter">精修后</button>
  </view>
  <view class="card notice">
    <view class="notice-title">水印预览已生成</view>
    <view class="muted">标准高清、无水印成片和完整参数报告将在阶段 C 的支付与交付计划中接入。</view>
  </view>
  <button class="button-primary" bindtap="adjustAgain">返回调整方向</button>
</view>
```

`pages/preview/index.wxss`：

```css
.preview { padding: 0; overflow: hidden; position: relative; }
.preview-image { display: block; width: 100%; height: 760rpx; background: #d8cec6; }
.preview-tag { position: absolute; top: 24rpx; left: 24rpx; padding: 10rpx 18rpx; border-radius: 999rpx; color: #fff; background: rgba(23,23,25,.62); font-size: 22rpx; }
.compare { display: grid; grid-template-columns: 1fr 1fr; gap: 16rpx; margin: 24rpx 0; }
.compare button { border-radius: 20rpx; color: #5d5862; background: #ece8e2; }
.compare button.active { color: #fff; background: #6c58c9; }
.notice { margin-bottom: 24rpx; }
.notice-title { margin-bottom: 10rpx; font-size: 30rpx; font-weight: 750; }
```

第一阶段不提供保存、价格和支付按钮。

- [ ] **Step 6: 完成视觉基线**

检查 Step 1–5 的 WXSS 已统一使用背景 `#F7F5F0`、主文字 `#171719`、强调色 `#6C58C9`、卡片圆角 `24rpx` 和主按钮高度 `96rpx`。逐页确认加载、禁用、成功和错误状态都有对应 WXML 分支。

- [ ] **Step 7: 微信开发者工具编译**

Run:

```text
使用微信开发者工具打开：
D:\Documents\workspace\projects\Project-002-修图AI小程序\04-源文件与代码\apps\miniprogram
```

Expected:

- 编译无错误。
- 首页可进入方案页。
- 方案页可创建任务。
- 实况页展示事件。
- 预览页可打开。

- [ ] **Step 8: 提交**

```powershell
git add "04-源文件与代码/apps/miniprogram"
git commit -m "feat: build edit trace vertical slice"
```

## Task 9: 全量验证与文档

**Files:**

- Create: `04-源文件与代码/README.md`
- Create: `06-复盘与踩坑/V1阶段A验证记录-v01-20260724.md`
- Modify: `README.md`
- Modify: `HANDOFF.md`

**Interfaces:**

- Consumes: Task 1–8 全部交付。
- Produces: 可复现启动说明、自动化证据、微信开发者工具证据和阶段复盘。

- [ ] **Step 1: 运行全量自动化验证**

Run:

```powershell
cd "D:\Documents\workspace\projects\Project-002-修图AI小程序\04-源文件与代码"
npm.cmd run typecheck
npm.cmd test
```

Expected:

- TypeScript exit code `0`。
- 所有 Vitest 测试 PASS。

- [ ] **Step 2: 运行 API**

Run:

```powershell
npm.cmd run dev:api
```

Expected:

- `http://127.0.0.1:3100` 开始监听。
- 创建任务、运行预览和读取事件均返回预期状态码。

- [ ] **Step 3: 微信开发者工具验证**

依次验证：

1. 首页加载。
2. 方案确认。
3. 创建任务。
4. AI 精修实况事件顺序。
5. 退出实况页再进入时事件不重复。
6. 进入水印预览。
7. API 停止时显示错误和重试按钮。

- [ ] **Step 4: 编写源码 README**

`04-源文件与代码/README.md`：

```markdown
# Project-002 V1 阶段 A 源码

## 环境

- Node.js v24.18.0 LTS
- npm v11.16.0
- 微信开发者工具

## 安装

\`\`\`powershell
cd "D:\Documents\workspace\projects\Project-002-修图AI小程序\04-源文件与代码"
npm.cmd install
\`\`\`

## 启动 API

\`\`\`powershell
npm.cmd run dev:api
\`\`\`

API 地址：`http://127.0.0.1:3100`

## 测试

\`\`\`powershell
npm.cmd run typecheck
npm.cmd test
\`\`\`

## 打开小程序

使用微信开发者工具打开：

`D:\Documents\workspace\projects\Project-002-修图AI小程序\04-源文件与代码\apps\miniprogram`

## 阶段边界

本阶段使用 `demo-portrait-001` 和确定性模拟供应商，只验证人像精修、任务状态机、AI 精修实况和水印预览纵向体验。尚未接入真实用户图片、真实供应商、微信支付、积分、审美档案和增长系统。
```

- [ ] **Step 5: 写验证记录并同步项目文档**

`06-复盘与踩坑/V1阶段A验证记录-v01-20260724.md` 使用以下内容；微信开发者工具版本一行必须替换为工具“关于”窗口实际显示的版本号，测试数量必须以命令输出为准：

```markdown
# V1 阶段 A 验证记录

- 验证日期：2026-07-24
- Node.js：v24.18.0 LTS
- npm：v11.16.0
- 微信开发者工具：记录实际版本号

## 自动化验证

\`\`\`powershell
npm.cmd run typecheck
npm.cmd test
\`\`\`

- 类型检查：通过
- Vitest：记录实际通过文件数和测试数

## 微信开发者工具验证

- [x] 首页进入方案确认页
- [x] 示例任务创建成功
- [x] AI 精修实况按服务端 sequence 展示
- [x] 重复事件未重复渲染
- [x] 水印预览页打开
- [x] API 停止后显示错误与重试

## 阶段限制

- 只使用示例资产和模拟供应商。
- 没有真实上传、审核、支付和长期持久化。
- 画质增强和杂物消除只存在共享契约。

## 阶段 B 进入条件

- 自动化和微信工具验证全部通过。
- 供应商专项任务提供至少一个候选测试环境。
- 部署地域和图片保留规则完成确认。
```

在项目根 `README.md` 进度表追加：

```markdown
| 2026-07-24 | 执行中 | 完成 V1 阶段 A：任务状态机、模拟供应商、AI 精修实况与水印预览纵向切片 | 进入真实上传、审核、供应商与质量闭环 |
```

在 `HANDOFF.md` 变更记录追加：

```markdown
| 2026-07-24 | v02.3 | 完成阶段 A 纵向切片，打通模拟人像精修任务、AI 精修实况和水印预览；记录自动化与微信开发者工具验证证据 | 已验证 |
```

- [ ] **Step 6: 最终提交**

```powershell
git add "04-源文件与代码/README.md" "06-复盘与踩坑/V1阶段A验证记录-v01-20260724.md" "README.md" "HANDOFF.md"
git commit -m "docs: verify edit trace vertical slice"
```

## Plan Self-Review

- PRD v02 的三项工具契约由 Task 2 覆盖；阶段 A 页面只开放人像精修，符合分阶段约束。
- AI 精修实况事件、真实性、增量恢复和敏感字段由 Task 2、4、5、7、8 覆盖。
- 状态机和非法迁移由 Task 3 覆盖。
- 供应商隔离由 Task 5 的 `ImageProvider` 覆盖。
- API 错误和增量读取由 Task 6 覆盖。
- 用户可操作纵向路径由 Task 8 覆盖。
- 自动化、微信工具验证和项目文档由 Task 9 覆盖。
- 支付、真实图片、审美档案和增长明确排除并进入后续独立阶段。
- 接口命名在任务间保持一致：`TaskService`、`TaskRepository`、`ImageProvider`、`mergeEvents`。
- 计划不存在未定义函数、`TODO`、`TBD` 或“适当处理”等占位说明。
