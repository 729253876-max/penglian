# Project-002 修图 AI 小程序｜Task 6 本地整改交接

> 版本：v05
>
> 日期：2026-08-20（Asia/Shanghai）
>
> 当前阶段：Fix A–F 本地整改、门禁验证与最终全差异静态复审已完成；本文件所在提交即 delivery commit，未 push，待 push 授权

## 1. 恢复时先读

1. 本文件。
2. `06-复盘与踩坑/PR-1质量门禁Task6本地整改验证记录-v05-20260820.md`。
3. `06-复盘与踩坑/PR-1独立全差异复审-v01-20260814.md`，特别是追加的 2026-08-20 Task 6 章节。
4. `02-方案与设计/实施计划/2026-08-15-PR质量门禁治理整改实施计划.md`。

旧 v04 的“Task 2–5 已完成/localhost-only 已落地”只反映当时范围；当前准确口径是：Fix A–F 已在**本文件所在 delivery commit** 完成并有相应自动化证据，网络守卫也仅覆盖加载 Vitest setup 的当前进程，不扩大为 child process、worker、OS 或独立信任域保证。

## 2. 现场身份：不得混称

- worktree：`.worktrees/v1-faithful-portrait-rescue`
- 分支：`codex/v1-faithful-portrait-rescue`
- reviewed working-tree base HEAD：`ee5de007da612bc86835a5105d79f6a535f6c0b1`
- report delivery commit：本文件所在提交（提交后以 `git rev-parse HEAD` 读取）
- upstream/latest PR head/latest CI run head：`a1853c1281538c2ef9f55b93e0b921bc5bb7144e`
- 修复：25 个终审文件已于 2026-08-23 按显式白名单纳入本文件所在提交。

恢复后只读核验：

```powershell
git status --short --branch
git rev-parse HEAD
git rev-parse '@{upstream}'
git diff --check
git diff --check origin/master
gh pr view 1 --repo 729253876-max/penglian --json state,isDraft,headRefOid,mergeable,mergeStateStatus,reviewDecision,statusCheckRollup,reviews
gh api repos/729253876-max/penglian/branches/master/protection
gh api repos/729253876-max/penglian/rulesets
```

不要把本地 base HEAD、delivery commit、PR head 或 CI run head 写成同一个“当前 HEAD”。

## 3. 本地已完成且无需重做

- Fix A：成功质量结果完整唯一覆盖五项冻结检查；失败部分语义保留；旧成功夹具已修复。
- Fix B：`yaml@2.9.0` 结构化 workflow 契约，覆盖 named inline/block/folded、额外 trigger/job/action/run 和 job permissions，fail-closed。
- Fix C：Vitest 当前进程网络守卫覆盖 net named、Socket、TLS、HTTP/HTTPS named；不覆盖 child/worker/OS。
- Fix D：live probe 已具备 pipe、有界诊断、error/exit 条件终止、close 等待和可取消 cleanup timer。
- Fix E：`nanoid` 精确 `3.3.18`；Fix E fresh clean-install audit 为 0 漏洞。
- Fix F：仅重型微信运行时同步测试局部 15 秒；并发全量稳定，构建正确性不放宽。
- 新鲜本地门禁：typecheck、Vitest（56 files passed/2 skipped；721 passed/14 skipped/0 failed）、小程序 runtime、微信 build、两项 diff check 均 PASS。
- 最终只读全工作树复审：0 个本地 Critical、0 个本地 Important；Fix A–F 均 CLOSED；单独 stage 授权已取得并完成。

### 3.1 Deferred Minor

- Fix B 仍可补一个“第三个本身已批准 action”的显式 mutation 测试。现有精确步骤数量与顺序已经拒绝该行为，因此只作为防回归覆盖 Minor 延后，不阻止 Fix B 关闭。
- Fix D 的 close-wait timer 活动句柄问题已在 round 1 修复并由 timer 清零测试覆盖；最终全差异复审未发现剩余 Fix D Minor。

## 4. 不能跳过的边界

- **NOT ACCEPTED**：13 个 MySQL、1 个 COS、微信 IDE/真机、真实 provider、API 重启恢复。
- PR #1 为 OPEN、Ready、MERGEABLE/CLEAN，`reviewDecision`/`reviews` 为空；旧 `quality-gate` success 只绑定 `a1853c1`。
- branch protection REST 为 404；active ruleset `20883280` include `master`，要求 strict `quality-gate`、0 approval、无 bypass、禁止删除/非快进与会话解决。
- **`BLOCKED_NEGATIVE_RULESET_TEST`**：没有 rule evaluation/dry-run，也未获授权创建临时测试 PR/分支；不可宣称 ruleset 不可绕过。
- collaborators 只见 `729253876-max` admin，`INDEPENDENT_REVIEWER_AVAILABLE=false`。
- 同源信任域 Important 仍未关闭：PR 可同时改 workflow、validator、tests，ruleset 仅认同名 GitHub Actions check。结构化契约不能替代独立信任域；不要建议 `pull_request_target` checkout PR code。

## 5. 继续工作的唯一顺序

1. 最终全差异静态复审、stage 与本文件所在 delivery commit 已完成；下一动作只能是请求单独 push 授权。
2. push 未获授权；禁止自动合并、强推、删除分支/worktree。
3. push 后，要求 `delivery commit == PR head == CI run head`，并运行该最新远端 CI。
4. ruleset 负向测试另提无害、最小范围方案，取得专门授权后才执行。
5. 没有独立人类 reviewer 时，除非用户书面接受剩余风险并选择替代信任方案，否则不得建议合并。

## 6. 持续禁止

- 不读取、打印或写入 Token、Cookie、凭据、真实 endpoint。
- 不接入真实模型、数据库、COS、微信正式 AppID、付费服务或生产部署。
- 不把本地测试、旧 CI、代理复审或 ruleset 配置扩大为真实服务、真机、独立人类批准或不可绕过证明。
