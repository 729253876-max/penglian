# Project-002 修图 AI 小程序｜新线程交接

> 版本：v02
>
> 日期：2026-08-14（Asia/Shanghai）
>
> 当前阶段：重要人像忠实救片闭环已实现；Draft PR 合并前质量门禁规格已写入，待用户书面复核后编写实施计划

## 1. 必须先核对的工作现场

- 规范项目：`D:\Documents\workspace\projects\Project-002-修图AI小程序`
- 当前独立 worktree：`D:\Documents\workspace\projects\Project-002-修图AI小程序\.worktrees\v1-faithful-portrait-rescue`
- 当前分支：`codex/v1-faithful-portrait-rescue`
- 本交接编写前的代码/设计基线：`5d6bbfd docs: design pull request quality gate`；提交本交接文件后 HEAD 会前进，新线程必须以实际 `git log` 为准
- 远程仓库：`https://github.com/729253876-max/penglian`
- Draft PR：`https://github.com/729253876-max/penglian/pull/1`
- 目标分支：`master`

新线程开始后必须重新运行以下只读命令，以上信息只能作为交接背景，不能代替最新证据：

```powershell
git status --short --branch
git log -3 --oneline
git remote -v
```

随后只读核对 PR #1 的 `isDraft`、head/base、checks、reviews 和远程 head SHA。不得在核对前宣称状态未变化。

## 2. 已完成且不得重做的工作

### 2.1 重要人像忠实救片产品闭环

Tasks 1–9 已实现、提交并完成任务级审查。核心结果：

- 只有属于当前用户、`APPROVED`、`NORMALIZED` 的上传资产可以创建人像任务。
- 两档固定方向：`NATURAL_RESCUE`（自然度 85、细节 35）和 `CLEAR_RESCUE`（自然度 75、细节 60）。
- provider 只提供候选与处理回执；质量判定和预览放行只由项目质量门禁产生。
- 质量失败最多重试一次；二次失败无预览且 `noCharge=true`。
- 生产装配使用 `UnavailableImageProvider`，不得使用 Mock 伪造成功；当前生产预览稳定收敛为 `PREVIEW_PROVIDER_FAILED`。
- 页面离开后可按连续事件序号恢复；API 进程重启恢复仍未实现。

### 2.2 用户重点：完整文字展示真实修复过程

这是持续有效的产品硬约束，后续任何工作不得弱化：

- 完整文字记录必须展示实际发生的上传、诊断、保护、方案、处理、质量、重试和交付事件。
- 不得输出或伪造模型隐藏推理，不得用定时器或固定脚本编造未发生步骤。
- 失败码、证据来源、最终快照和事件顺序必须一致；不完整或冲突的证据不得展示成功预览。

最终修复提交 `d16f2f1` 已严格绑定：

- `PREVIEW_PROVIDER_FAILED` → `SYSTEM_CHECK`
- `FIDELITY_GATE_FAILED` → `QUALITY_GATE`
- `PORTRAIT_NOT_SUITABLE`、`ASSET_NOT_APPROVED` 是创建前拒绝，不得伪装为 `TASK_FAILED` 日志。

### 2.3 最近自动化证据

在 `d16f2f1` 上取得的最新完整代码门禁：

- 全仓 typecheck：通过。
- Vitest：56 个文件，54 passed、2 skipped；654 项测试，640 passed、14 skipped、0 failed。
- 小程序 runtime verification：通过。
- WeChat TypeScript build：通过。
- `git diff --check`：通过。

14 项 skipped 必须继续标记为未验收：

- MySQL integration 13 项：缺少 `MYSQL_INTEGRATION_URL`，`NOT ACCEPTED`。
- object storage 环境条件项 1 项：没有真实 COS 证据，`NOT ACCEPTED`。

提交 `5d6bbfd` 只新增设计文档，没有修改业务代码。

## 3. GitHub 与 PR 当前状态

- 空 GitHub 仓库已初始化：本地 `master` 已普通推送并设为默认分支，没有强推。
- 当前功能分支已推送并跟踪 `origin/codex/v1-faithful-portrait-rescue`。
- PR #1 当前为 Draft；本交接提交前最后一次核对时可合并，但没有 GitHub Actions checks、reviews 或 comments。推送本交接提交后必须重新核对远程 head 和 PR 状态。
- GitHub CLI 2.97.0 已按当前用户范围安装；账号 `729253876-max` 的凭据存放在 Windows keyring。禁止读取、打印或写入 Token，禁止导入浏览器 Cookie。
- 不得自动合并、删除 worktree、删除分支或强推。

## 4. 当前批准状态

用户已逐节批准“最小可信 CI”和“独立复审 + 转 Ready 门禁”的设计方向。

书面规格已创建、完成自审、提交并推送：

`02-方案与设计/PR合并前质量门禁设计-v01-20260814.md`

提交：`5d6bbfd docs: design pull request quality gate`

当前流程停在“请用户复核书面规格”。用户尚未在本线程回复“批准规格”，因此新线程不能把实施计划或 CI 实现写成已经获批；若用户在新线程明确批准书面规格，再进入 `superpowers:writing-plans`。

## 5. 下一步准确顺序

1. 完整读取本交接文件和书面规格。
2. 只读核对 Git、远程分支和 PR #1 最新状态。
3. 用户明确批准书面规格后，使用 `superpowers:writing-plans` 编写详细实施计划，保存到 `02-方案与设计/实施计划/`。
4. 实施计划必须把工作拆为：工作流安全契约测试/静态检查、固定 Action SHA 的 CI 工作流、实际 PR 运行验证、独立全差异复审、Critical/Important 修复循环、Ready 条件复核。
5. 实施计划需用户批准后才实现 `.github/workflows/...`；不得把“继续任务”扩大为自动合并。
6. CI 全绿且独立复审无未解决 Critical/Important 后，才可将 PR 转为 Ready；转 Ready 也不等于获准合并。

## 6. 已批准的 CI 设计边界

- 触发：目标为 `master` 的 `pull_request`，另允许 `workflow_dispatch`。
- 权限：顶层只授予 `contents: read`；禁止 `pull_request_target`。
- runner：GitHub 托管 Windows runner。
- 依赖：固定 Node.js 主版本，在 `04-源文件与代码` 使用 `npm.cmd ci`；第三方 Actions 固定到人工核对的完整 commit SHA。
- 门禁：typecheck、Vitest、runtime verification、WeChat TypeScript build、`git diff --check`。
- 不读取 GitHub Secrets，不连接 MySQL/COS/STS/模型，不上传用户照片，不部署、不发布、不迁移数据库。
- 14 项环境跳过继续保持 `NOT ACCEPTED`，不能写成真实环境 PASS。

## 7. 进入下一阶段前的风险门禁

当前仍未验收：

- 真实模型质量与成本：`NOT RUN / NOT ACCEPTED`。
- 真实 MySQL/COS：`NOT RUN / NOT ACCEPTED`。
- 微信 IDE、正式 AppID、Android/iOS 真机、弱网和发布：`NOT RUN / NOT ACCEPTED`。
- API 进程重启恢复：`NOT IMPLEMENTED`。
- MySQL 任务/事件事务与 crash atomicity：B3 parked。

不得在本 PR 继续叠加真实模型 PoC 或 B3/B4 功能。真实模型 PoC 必须新建独立规格、计划、分支/worktree，并重新审查代码与权重许可证、下载目标、依赖安装、RTX 4060 8GB 显存、真实照片授权、数据外传、删除条款和费用；没有新的明确批准不得下载模型、安装 Python/Torch 或调用外部 provider。

## 8. 新线程禁止事项

- 不重做 Tasks 1–9 或已完成的 TDD 修复。
- 不把旧 B1 交接当成当前 HEAD 或当前下一步。
- 不把本地自动化通过扩大为真实云、模型、微信真机或发布通过。
- 不伪造完整文字修复步骤、隐藏推理或证据标签。
- 不自动启用 GitHub Secrets、真实照片、付费服务、云资源或生产部署。
- 不自动合并 PR，不强推，不删除 worktree 或分支。

## 9. 新线程建议首条指令

```text
请完整阅读：
D:\Documents\workspace\projects\Project-002-修图AI小程序\.worktrees\v1-faithful-portrait-rescue\05-输出与交付\新线程交接-PR质量门禁待实施-v02-20260814.md

并继续 Project-002 修图 AI 小程序。先只读核对当前 worktree、分支、HEAD、远程分支和 GitHub Draft PR #1 的最新状态；不要重做已完成的 Tasks 1–9，也不要自动合并、强推、删除 worktree、接入真实模型/云服务或读取凭据。

我现在批准书面规格 `02-方案与设计/PR合并前质量门禁设计-v01-20260814.md`。请使用 `superpowers:writing-plans` 编写 PR 合并前质量门禁的详细实施计划，重点保证“完整文字展现真实修复过程”只能来自实际事件和真实证据。计划写完先交我审阅，不要直接实现 CI。
```
