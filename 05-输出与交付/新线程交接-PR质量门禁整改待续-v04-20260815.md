# Project-002 修图 AI 小程序｜新线程交接

> 版本：v04
>
> 日期：2026-08-15（Asia/Shanghai）
> 当前阶段：Task 2（多行 `run` 命令绕过）已完成；Task 3 通过回归，待用户确认下一步

## 1. 恢复时必须完整阅读

1. 本交接文件。
2. `06-复盘与踩坑/PR-1质量门禁自检与暂停记录-v04-20260815.md`。
3. `02-方案与设计/实施计划/2026-08-15-PR质量门禁治理整改实施计划.md`。
4. `06-复盘与踩坑/PR-1独立全差异复审-v01-20260814.md`（如需继续 Task 6）。

不得只看旧版本交接；v04 为当前续接边界。

## 2. 恢复现场核验（只读）

请在继续前先逐条复核以下命令结果是否仍成立：

```powershell
git status --short --branch
git log -5 --oneline --decorate
git rev-parse HEAD
git rev-parse '@{upstream}'
gh pr view 1 --repo 729253876-max/penglian --json state,isDraft,headRefOid,mergeable,mergeStateStatus,reviewDecision,statusCheckRollup,reviews
gh api repos/729253876-max/penglian/branches/master/protection
gh api repos/729253876-max/penglian/rulesets
```

## 3. 本轮已完成（不得重做）

- 忠实救片 Tasks 1–9 全部已完成，不得重做。
- `quality-gate` 静态契约与规则：
  - actions SHA 固定；
  - `contents: read`；
  - `pull_request` + `workflow_dispatch`；
  - `windows-latest` + 固定命令链路 + `git diff --check`。
- `Task 1`：ruleset `codex-pr-quality-gate-master` 已建立并回读验真，`quality-gate` 必要检查已生效。
- `Task 2`：workflow 多行 `run` 命令绕过已修复：
  - `@photo-ai/ci-contract` 合约扫描识别 `run: |` 与 `run: >` 远程命令；
  - `secrets` 与 `MYSQL_INTEGRATION_URL/COS_SECRET/STS_SECRET/MODEL_API_KEY` 命中；
  - 对应测试通过。
- `Task 3`：测试进程 localhost-only 出站网络门禁已落地（4/56 项通过）。

## 4. 关键边界与风险

- 当前 PR #1 仍为 `open`、`READY`、`mergeable`，近次 `quality-gate` 成功，但 `reviewDecision` 为空。
- `master` 分支未启用 branch protection，依赖 ruleset 阻断；
- `INDEPENDENT_REVIEWER_AVAILABLE` 仍未确认为可靠独立 reviewer 场景。
- `nanoid@3.3.16` advisory 仍未处置。
- MySQL/COS 真实环境测试继续 `NOT ACCEPTED`。

## 5. 下一步优先级（用户确认后）

1. `Task 4`：Node/npm 工具链与 live probe 约束（含 2 秒定时回归）。
2. `Task 5`：高危依赖治理决策并形成接受或升级记录。
3. `Task 6`：全量复审与最终暂停确认。

## 6. 持续禁止

- 不自动合并 PR、强推、删除 worktree/分支。
- 不接入真实模型、真实数据库、COS、微信正式 AppID、付费服务。
- 不读取/打印凭据或 Cookie。
- 不把代理复审/自动评论当作独立人类批准。
- 不把本 PR 自动检测通过扩展为生产、真机或未授权业务功能。
