# Project-002 修图 AI 小程序｜新线程交接

> 版本：v03
>
> 日期：2026-08-15（Asia/Shanghai）
> 当前阶段：PR 质量检查已实现并转 Ready；自检发现 master 未保护，项目暂停，待治理整改

## 1. 恢复时必须完整阅读

1. 本交接文件。
2. `06-复盘与踩坑/PR-1质量门禁自检与暂停记录-v01-20260815.md`。
3. `02-方案与设计/实施计划/2026-08-15-PR质量门禁治理整改实施计划.md`。
4. `06-复盘与踩坑/PR-1独立全差异复审-v01-20260814.md`。

不得只看旧 v02 交接；v02 的 Draft/待实施状态已过时。

## 2. 暂停时现场

- 项目：`D:\Documents\workspace\projects\Project-002-修图AI小程序`
- worktree：`D:\Documents\workspace\projects\Project-002-修图AI小程序\.worktrees\v1-faithful-portrait-rescue`
- 分支：`codex/v1-faithful-portrait-rescue`
- 交接编写前 HEAD/upstream/PR head：`b9dbefe423d161b2a72ac653fff555982d6c7a7a`
- PR #1：<https://github.com/729253876-max/penglian/pull/1>
- PR：Open、Ready、mergeable；无 GitHub reviews
- 最新 CI：<https://github.com/729253876-max/penglian/actions/runs/31819710296>，success，671 passed / 14 skipped / 0 failed
- master：未配置 branch protection
- repository rulesets：空

本交接提交后 HEAD 会变化，以上 SHA 只能作为背景。恢复时必须重新运行：

```powershell
git status --short --branch
git log -5 --oneline --decorate
git rev-parse HEAD
git rev-parse '@{upstream}'
gh pr view 1 --repo 729253876-max/penglian --json state,isDraft,headRefOid,mergeable,mergeStateStatus,reviewDecision,statusCheckRollup,reviews
gh api repos/729253876-max/penglian/branches/master/protection
gh api repos/729253876-max/penglian/rulesets
```

## 3. 已完成且不得重做

- 忠实救片 Tasks 1–9 及其真实性修复。
- PR Quality Gate workflow、固定 Action SHA、`contents: read`、`npm ci --ignore-scripts`。
- 完整 `origin/master...HEAD` whitespace 检查。
- 4 个独立复审 Important 修复：provider accessor/TOCTOU、完整 journal、snapshot 终态不变量、full-refetch 前缀不可改写。
- PR 描述更新和 Draft → Ready。
- 当前自动化范围内的 0 unresolved Critical / 0 unresolved Important。

## 4. 最高优先级纠偏

“有绿色 check”不等于“有合并门禁”。master 未保护且 rulesets 为空，PR 即使没有人类 review 仍显示 CLEAN/MERGEABLE。因此：

- 当前不建议合并。
- 恢复后先执行整改计划 Task 0，不直接改代码或 GitHub 设置。
- ruleset 是外部写操作，必须再次取得用户明确授权。
- 若只有 PR 作者一个账号，不能机械启用 1 个 required approval；先确认独立 reviewer，否则会治理死锁。
- 若没有独立 reviewer，在用户书面接受“无独立人类批准”的剩余风险前仍不得建议合并；代理复审不能替代该风险接受。

## 5. 待整改任务顺序

1. 重取现场并决定是否把暂停中的 PR 退回 Draft。
2. 设计、批准、创建 master ruleset，使 `quality-gate` 成为 required check。
3. TDD 修复多行 `run` 静态契约绕过。
4. 建立 Vitest localhost-only 出站网络门禁。
5. 固定 Node/npm 精确工具链并修复 2 秒 live probe 时序。
6. 独立治理 `nanoid@3.3.16` high advisory；禁止直接 `npm audit fix`。
7. 全量 CI、独立复审、ruleset 负向验证，随后再次等待合并授权。

## 6. 未验收边界

- 13 项 MySQL integration：`NOT ACCEPTED`。
- 1 项 object-storage/COS 环境测试：`NOT ACCEPTED`。
- 真实模型质量与成本：`NOT RUN / NOT ACCEPTED`。
- 微信 IDE、正式 AppID、真机、弱网、发布：`NOT RUN / NOT ACCEPTED`。
- API 进程重启恢复：`NOT IMPLEMENTED`。
- 当前生产无真实 provider。
- 依赖安全：1 个既有 high advisory，未关闭。

## 7. 禁止事项

- 不自动合并 PR、强推、删除 worktree/分支。
- 不自动修改 ruleset、branch protection、reviewer、PR Draft/Ready 状态。
- 不读取 Token/Cookie，不接入真实模型/云服务/真实数据库。
- 不把代理复审报告称为 GitHub 人类批准。
- 不把 CI 通过扩大为生产、真机或真实修图质量通过。
- 不重做 Tasks 1–9，不在整改 PR 中夹带 B3/B4 或新产品功能。

## 8. 建议恢复指令

```text
请完整阅读：
D:\Documents\workspace\projects\Project-002-修图AI小程序\.worktrees\v1-faithful-portrait-rescue\05-输出与交付\新线程交接-PR质量门禁整改待续-v03-20260815.md

并继续 Project-002 修图 AI 小程序。先只读核对 worktree、分支、HEAD/upstream、PR #1、最新 checks/reviews、master branch protection 和 repository rulesets。不要重做 Tasks 1–9，不要自动合并、强推、删除 worktree/分支、接入真实模型/云服务或读取凭据。

请先审阅 `02-方案与设计/实施计划/2026-08-15-PR质量门禁治理整改实施计划.md`，重新评估独立 reviewer 是否可用以及 ruleset 是否会造成审批死锁；在我明确批准 GitHub 外部状态写操作前，只做只读核对和计划修订，不直接修改 ruleset 或代码。
```
